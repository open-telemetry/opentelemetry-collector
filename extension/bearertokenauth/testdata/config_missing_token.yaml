extensions:
  bearer:

# Data pipeline is required to load the config.
receivers:
  nop:
processors:
  nop:
exporters:
  nop:

service:
  extensions: [bearer]
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
