extensions:
  oauth2clientcredentials/1:
    client_id: someclientid
    client_secret: someclientsecret
    token_url: https://someserver.com/oauth2/default/v1/token
    scopes: ["api.metrics"]
    timeout: 1s

  oauth2clientcredentials/withtls:
    client_id: someclientid2
    client_secret: someclientsecret2
    token_url: https://someserver2.com/oauth2/default/v1/token
    scopes: ["api.metrics"]
    timeout: 1s
    # tls settings for the token client
    tls:
      insecure: true
      ca_file: cafile
      cert_file: certfile
      key_file: keyfile



# Data pipeline is required to load the config.
receivers:
  nop:
processors:
  nop:
exporters:
  nop:

service:
  extensions: [oauth2clientcredentials/1, oauth2clientcredentials/withtls]
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
