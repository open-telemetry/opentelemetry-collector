extensions:
  oauth2clientcredentials/missingurl:
    client_id: someclientid
    client_secret: someclientsecret
    scopes: ["api.metrics"]

# Data pipeline is required to load the config.
receivers:
  nop:
processors:
  nop:
exporters:
  nop:

service:
  extensions: [oauth2clientcredentials/missingurl]
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
