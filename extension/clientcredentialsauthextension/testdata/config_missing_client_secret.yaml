extensions:
  oauth2clientcredentials/missingseceret:
    client_id: someclientid
    token_url: https://someserver.com/oauth2/default/v1/token
    scopes: ["api.metrics"]

# Data pipeline is required to load the config.
receivers:
  nop:
processors:
  nop:
exporters:
  nop:

service:
  extensions: [oauth2clientcredentials/missingseceret]
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
