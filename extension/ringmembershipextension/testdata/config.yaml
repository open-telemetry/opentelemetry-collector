extensions:
  ring_membership:
  ring_membership/1:
    peer_discovery_dns_name: "random.dns.name"

service:
  extensions: [ring_membership/1]
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [exampleexporter]

# Data pipeline is required to load the config.
receivers:
  examplereceiver:
processors:
  exampleprocessor:
exporters:
  exampleexporter:
