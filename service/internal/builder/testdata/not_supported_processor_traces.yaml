receivers:
  examplereceiver:
processors:
  bf:
exporters:
  exampleexporter:

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [bf]
      exporters: [exampleexporter]
