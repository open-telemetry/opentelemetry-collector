receivers:
  examplereceiver:
processors:
  bf:
exporters:
  exampleexporter:

service:
  pipelines:
    metrics:
      receivers: [examplereceiver]
      processors: [bf]
      exporters: [exampleexporter]
