receivers:
  otlp:
    protocols:
      grpc:

processors:
  queued_retry: {}

exporters:
  otlp:
    endpoint: "1.2.3.4:1234"

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [queued_retry]
      exporters: [otlp]
