receivers:
  nop:

processors:
  nop:

exporters:
  nop:

extensions:
  nop:

service:
  telemetry:
    metrics:
      namespace: customNamespace
  extensions: [nop]
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
    logs:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
