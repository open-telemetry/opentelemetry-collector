receivers:
  nop:

processors:
  nop:

exporters:
  nop: l

extensions:
  nop: 9

service:
  telemetry:
    metrics:
      address: localhost:8888
  extensions: [nop]
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
    metrics:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
    logs:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
