receivers:
  otlp:
    protocols:
      grpc:

exporters:
  otlp:
    endpoint: "locahost:55678"

processors:
  batch:

extensions:
  pprof:
  zpages:

service:
  extensions: [pprof, zpages]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlp]
