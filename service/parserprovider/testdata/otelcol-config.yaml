receivers:
  otlp:
    protocols:
      grpc:

exporters:
  otlp:
    endpoint: "locahost:55678"

processors:
  attributes:
    actions:
      - key: key1
        value: 123
        action: insert
  batch:

extensions:
  health_check: {}
  pprof:
  zpages:

service:
  extensions: [pprof, zpages, health_check]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [attributes, batch]
      exporters: [otlp]
