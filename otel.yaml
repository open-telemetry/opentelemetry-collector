receivers:
  nop:
    

exporters:
  influxdb:
    endpoint: null
    org: null
    bucket: "production"
    token: "${INFLUXD_TOKEN}"

service:
  pipelines:
    metrics:
      receivers: [nop]
      exporters: [influxdb]
    