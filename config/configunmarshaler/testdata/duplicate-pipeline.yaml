receivers:
  examplereceiver:
exporters:
  exampleexporter:
processors:
  exampleprocessor:
service:
  pipelines:
    traces /pipe:
      receivers: [examplereceiver]
      exporters: [exampleexporter]
      processors: [exampleprocessor]
    traces/ pipe :
      receivers: [examplereceiver]
      exporters: [exampleexporter]
      processors: [exampleprocessor]
