receivers:
  examplereceiver:
exporters:
  exampleexporter:
processors:
  exampleprocessor/ proc:
  exampleprocessor /proc :
service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      exporters: [exampleexporter]
      processors: [exampleprocessor]
