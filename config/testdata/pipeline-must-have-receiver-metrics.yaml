receivers:
  examplereceiver:
    extra: "not present in the service pipeline"
exporters:
  exampleexporter:
processors:
  exampleprocessor:
service:
  pipelines:
    metrics:
      processors: [exampleprocessor]
      exporters: [exampleexporter]
