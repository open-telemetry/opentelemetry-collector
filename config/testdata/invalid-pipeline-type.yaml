receivers:
  examplereceiver:
processors:
  exampleprocessor:
exporters:
  exampleexporter:

service:
  pipelines:
    wrongdatatype:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [exampleexporter]
