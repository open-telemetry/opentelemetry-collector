receivers:
  examplereceiver:
exporters:
  exampleexporter:
processors:
  exampleprocessor:
  exampleprocessor/:
service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [exampleexporter]
