receivers:
  examplereceiver:
exporters:
  exampleexporter:
processors:
  exampleprocessor:
    tests
service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      exporters: [exampleexporter]
      processors: [exampleprocessor]
