receivers:
  multireceiver:
exporters:
  exampleexporter:
processors:
  exampleprocessor:
    tests
service:
  pipelines:
    traces:
      receivers: [multireceiver]
      exporters: [exampleexporter]
      processors: [exampleprocessor]
