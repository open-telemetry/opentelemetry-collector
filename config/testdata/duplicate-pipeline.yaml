receivers:
  examplereceiver:
exporters:
  exampleexporter:
processors:
  exampleprocessor:
pipelines:
  traces/default:
    receivers: [examplereceiver]
    exporters: [exampleexporter]
    processors: [exampleprocessor]
  traces/ default:
    receivers: [examplereceiver]
    exporters: [exampleexporter]
    processors: [exampleprocessor]
