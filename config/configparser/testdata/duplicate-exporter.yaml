receivers:
  examplereceiver:
exporters:
  exampleexporter/exp:
  exampleexporter/ exp :
processors:
  exampleprocessor:
service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      exporters: [exampleexporter]
      processors: [exampleprocessor]
