extensions:
  zpages:
    endpoint: "localhost:55680"

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:9318"
processors:

exporters:
  debug:

service:
  telemetry:
    metrics:
      level: none
  extensions: [zpages]
  pipelines:
    traces:
      receivers:
        - otlp
      processors: []
      exporters:
        - debug
