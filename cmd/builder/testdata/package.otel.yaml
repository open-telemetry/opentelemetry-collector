extensions:
  zpages:
    endpoint: "localhost:55699"

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:8318"
processors:

exporters:
  debug:

service:
  telemetry:
    metrics:
      level: none
  extensions: [zpages]
  pipelines:
    traces:
      receivers:
        - otlp
      processors: []
      exporters:
        - debug
