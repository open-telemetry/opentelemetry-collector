{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "any_field": {
      "description": "AnyField is an interface type.",
      "type": "object"
    },
    "count": {
      "description": "Count is a numeric counter.",
      "type": "integer"
    },
    "embedded_field": {
      "description": "EmbeddedField is a field from the embedded struct.",
      "type": "string"
    },
    "enabled": {
      "description": "Enabled controls whether the component is enabled.",
      "type": "boolean"
    },
    "fixed_array": {
      "description": "FixedArray is a fixed-size array.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "int_metadata": {
      "additionalProperties": {
        "type": "integer"
      },
      "description": "IntMetadata is a map with integer values.",
      "type": "object"
    },
    "large_uint": {
      "description": "LargeUint tests uint64 type.",
      "type": "integer"
    },
    "metadata": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Metadata is a key-value map.",
      "type": "object"
    },
    "name": {
      "description": "Name is the name of the component.",
      "type": "string"
    },
    "nested": {
      "description": "Nested contains nested configuration.",
      "properties": {
        "deep_nested": {
          "description": "DeepNested contains deeply nested configuration.",
          "properties": {
            "value": {
              "description": "Value is a configuration value.",
              "type": "string"
            }
          },
          "type": "object"
        },
        "host": {
          "description": "Host is the server host.",
          "type": "string"
        },
        "port": {
          "description": "Port is the server port.",
          "type": "integer"
        }
      },
      "type": "object"
    },
    "nested_ptr": {
      "description": "NestedPtr is a pointer to nested configuration.",
      "properties": {
        "deep_nested": {
          "description": "DeepNested contains deeply nested configuration.",
          "properties": {
            "value": {
              "description": "Value is a configuration value.",
              "type": "string"
            }
          },
          "type": "object"
        },
        "host": {
          "description": "Host is the server host.",
          "type": "string"
        },
        "port": {
          "description": "Port is the server port.",
          "type": "integer"
        }
      },
      "type": "object"
    },
    "numbers": {
      "description": "Numbers is a list of integers.",
      "items": {
        "type": "integer"
      },
      "type": "array"
    },
    "old_field": {
      "deprecated": true,
      "description": "OldField is deprecated and should not be used.",
      "type": "string"
    },
    "rate": {
      "description": "Rate is a floating point rate.",
      "type": "number"
    },
    "small_float": {
      "description": "SmallFloat tests float32 type.",
      "type": "number"
    },
    "small_int": {
      "description": "SmallInt tests int8 type.",
      "type": "integer"
    },
    "tags": {
      "description": "Tags is a list of tags.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "timeout": {
      "description": "Timeout is the timeout duration.",
      "pattern": "^[0-9]+(ns|us|µs|ms|s|m|h)$",
      "type": "string"
    }
  },
  "type": "object"
}