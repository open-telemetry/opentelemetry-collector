receivers:
  jaeger:
    enabled: true

exporters:
  opencensus:
    endpoint: "locahost:55678"
    enabled: true

processors:
  attributes:
    enabled: true
  queued-retry:
    enabled: true

pipelines:
  traces:
    receivers: [jaeger]
    processors: [attributes, queued-retry]
    exporters: [opencensus]
