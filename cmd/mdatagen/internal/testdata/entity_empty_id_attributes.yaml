type: sample
status:
  class: receiver
  stability:
    stable: [metrics]

resource_attributes:
  host.name:
    description: The hostname
    type: string
    enabled: true

entities:
  - type: host
    brief: A host instance.
    stability: stable
    identity: []
    description:
      - ref: host.name
