type: metric

status:
  class: receiver
  stability:
    beta: [traces, logs, metrics]
telemetry:
  metrics:
    request_latency:
      description: Latency (in microseconds) of a given sampling policy
      unit: µs
      enabled: true
      histogram:
