type: sample
display_name: Sample Component
status:
  class: receiver
  stability:
    stable: [metrics]

resource_attributes:
  host.id:
    description: The host identifier
    type: string
    enabled: true

entities:
  - type: host
    brief: A host instance.
    stability: stable
    identity:
      - ref: host.id
    description:
      - ref: host.missing
