type: metric

status:
  class: receiver
  stability:
    beta: [traces, logs, metrics]
telemetry:
  metrics:
    request_latency:
      description: Latency (in microseconds) of a given sampling policy
      unit: µs
      enabled: true
      stability: alpha
      histogram:
        value_type: int
        aggregation: invalid_aggregation_type
