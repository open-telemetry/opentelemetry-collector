// Code generated by mdatagen. DO NOT EDIT.

package {{ .Package }}

import (
	"path/filepath"
	"testing"

	"github.com/google/go-cmp/cmp"
	"github.com/google/go-cmp/cmp/cmpopts"
	{{- if and .OverrideValueEnabled .ResourceAttributes }}
	"github.com/stretchr/testify/assert"
	{{- end }}
	"github.com/stretchr/testify/require"

	{{- if .Metrics }}
	"go.opentelemetry.io/collector/confmap"
	{{- end }}
	"go.opentelemetry.io/collector/confmap/confmaptest"
)

{{ if .Metrics }}
func TestMetricsBuilderConfig(t *testing.T) {
	tests := []struct {
		name string
		want MetricsBuilderConfig
	}{
		{
			name: "default",
			want: NewDefaultMetricsBuilderConfig(),
		},
		{
			name: "all_set",
			want: MetricsBuilderConfig{
					Metrics: MetricsConfig{
						{{- range $name, $metric := .Metrics }}
						{{- $metricReag := hasAggregatableAttributes $metric.Attributes }}
						{{ $name.Render }}: {{ $name.Render }}MetricConfig{
							Enabled: true,
						{{- if $metricReag }}
						AggregationStrategy: AggregationStrategy{{ if eq $metric.Data.Type "Sum" }}Sum{{ else }}Avg{{ end }},
						EnabledAttributes:   []{{ $name.Render }}MetricAttributeKey{ {{- range $index, $element := $metric.Attributes -}}{{ if $index }}, {{ end }}{{ $name.Render }}MetricAttributeKey{{ $element.Render }}{{ end -}} },
						{{- end }}
					},
					{{- end }}
				},
				{{- if .ResourceAttributes }}
				ResourceAttributes: ResourceAttributesConfig{
					{{- range $name, $_ := .ResourceAttributes }}
					{{ $name.Render }}: {{ if $.OverrideValueEnabled }}{{ $name.Render }}{{ end }}ResourceAttributeConfig{Enabled: true},
					{{- end }}
				},
				{{- end }}
			},
		},
		{
			name: "none_set",
			want: MetricsBuilderConfig{
					Metrics: MetricsConfig{
						{{- range $name, $metric := .Metrics }}
						{{- $metricReag := hasAggregatableAttributes $metric.Attributes }}
						{{ $name.Render }}: {{ $name.Render }}MetricConfig{
							Enabled: false,
						{{- if $metricReag }}
						AggregationStrategy: AggregationStrategy{{ if eq $metric.Data.Type "Sum" }}Sum{{ else }}Avg{{ end }},
						EnabledAttributes:   []{{ $name.Render }}MetricAttributeKey{ {{- range $index, $element := $metric.Attributes -}}{{ if $index }}, {{ end }}{{ $name.Render }}MetricAttributeKey{{ $element.Render }}{{ end -}} },
						{{- end }}
					},
					{{- end }}
				},
				{{- if .ResourceAttributes }}
				ResourceAttributes: ResourceAttributesConfig{
					{{- range $name, $_ := .ResourceAttributes }}
					{{ $name.Render }}: {{ if $.OverrideValueEnabled }}{{ $name.Render }}{{ end }}ResourceAttributeConfig{Enabled: false},
					{{- end }}
				},
				{{- end }}
			},
		},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			cfg := loadMetricsBuilderConfig(t, tt.name)
			diff := cmp.Diff(tt.want, cfg, cmpopts.IgnoreUnexported(
				{{- range $name, $metric := .Metrics }}{{ $name.Render }}MetricConfig{}, {{ end }}
				{{- if $.OverrideValueEnabled }}
				{{- range $name, $_ := .ResourceAttributes }}{{ $name.Render }}ResourceAttributeConfig{}, {{ end }}
				{{- else }}
				{{- if .ResourceAttributes }}ResourceAttributeConfig{},{{ end }}
				{{- end }}
			))
			require.Emptyf(t, diff, "Config mismatch (-expected +actual):\n%s", diff)
		})
	}
}

{{- range $name, $metric := .Metrics }}
{{ if and $reag (hasAggregatableAttributes $metric.Attributes) -}}
func Test{{ $name.Render }}MetricsConfig_Validate(t *testing.T) {
  cfg := DefaultMetricsConfig().{{ $name.Render }}
  require.NoError(t, cfg.Validate())

  cfg.EnabledAttributes = []{{ $name.Render }}MetricAttributeKey{"invalid"}
  require.ErrorContains(t, cfg.Validate(), "metric {{ $name }} doesn't have an attribute invalid, valid attributes: [{{ range $index, $element := $metric.Attributes -}}{{ if $index }}, {{ end }}{{ (attributeInfo $element).Name }}{{ end }}]")

  cfg = DefaultMetricsConfig().{{ $name.Render }}
  cfg.AggregationStrategy = "invalid"
  require.ErrorContains(t, cfg.Validate(), "invalid aggregation strategy")
}
{{ end }}
{{ end }}

func loadMetricsBuilderConfig(t *testing.T, name string) MetricsBuilderConfig {
	cm, err := confmaptest.LoadConf(filepath.Join("testdata", "config.yaml"))
	require.NoError(t, err)
	sub, err := cm.Sub(name)
	require.NoError(t, err)
	cfg := NewDefaultMetricsBuilderConfig()
	require.NoError(t, sub.Unmarshal(&cfg, confmap.WithIgnoreUnused()))
	return cfg
}
{{- end }}

{{ if .Events }}
func loadLogsBuilderConfig(t *testing.T, name string) LogsBuilderConfig {
	cm, err := confmaptest.LoadConf(filepath.Join("testdata", "config.yaml"))
	require.NoError(t, err)
	sub, err := cm.Sub(name)
	require.NoError(t, err)
	cfg := DefaultLogsBuilderConfig()
	require.NoError(t, sub.Unmarshal(&cfg, confmap.WithIgnoreUnused()))
	return cfg
}
{{- end }}

{{ if .ResourceAttributes -}}
func TestResourceAttributesConfig(t *testing.T) {
	tests := []struct {
		name string
		want ResourceAttributesConfig
	}{
		{
			name: "default",
			want: DefaultResourceAttributesConfig(),
		},
		{
			name: "all_set",
			want: ResourceAttributesConfig{
				{{- range $name, $_ := .ResourceAttributes }}
				{{ $name.Render }}: {{ if $.OverrideValueEnabled }}{{ $name.Render }}{{ end }}ResourceAttributeConfig{Enabled: true},
				{{- end }}
			},
		},
		{
			name: "none_set",
			want: ResourceAttributesConfig{
				{{- range $name, $_ := .ResourceAttributes }}
				{{ $name.Render }}: {{ if $.OverrideValueEnabled }}{{ $name.Render }}{{ end }}ResourceAttributeConfig{Enabled: false},
				{{- end }}
			},
		},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			cfg := loadResourceAttributesConfig(t, tt.name)
			diff := cmp.Diff(tt.want, cfg, cmpopts.IgnoreUnexported(
				{{- if $.OverrideValueEnabled }}
				{{- range $name, $_ := .ResourceAttributes }}{{ $name.Render }}ResourceAttributeConfig{}, {{ end }}
				{{- else }}ResourceAttributeConfig{}, {{ end }}
			))
			require.Emptyf(t, diff, "Config mismatch (-expected +actual):\n%s", diff)
		})
	}
}

{{- if $.OverrideValueEnabled }}

func TestResourceAttributesOverrideConfig(t *testing.T) {
	cfg := loadResourceAttributesConfig(t, "override_set")
	{{- range $name, $_ := .ResourceAttributes }}
	assert.NotNil(t, cfg.{{ $name.Render }}.OverrideValue, "override_value should be set for {{ $name }}")
	{{- end }}
}

{{ $hasEnum := false }}
{{- range $_, $attr := .ResourceAttributes }}{{- if $attr.Enum }}{{ $hasEnum = true }}{{- end }}{{- end }}
{{- if $hasEnum }}
func TestResourceAttributesOverrideEnumValidation(t *testing.T) {
	{{- range $name, $attr := .ResourceAttributes }}
	{{- if $attr.Enum }}
	{
		invalidVal := "invalid-enum-value"
		cfg := {{ $name.Render }}ResourceAttributeConfig{
			Enabled: true,
			OverrideValue: &invalidVal,
		}
		assert.Error(t, cfg.Validate())
	}
	{{- end }}
	{{- end }}
}
{{- end }}
{{- end }}

func loadResourceAttributesConfig(t *testing.T, name string) ResourceAttributesConfig {
	cm, err := confmaptest.LoadConf(filepath.Join("testdata", "config.yaml"))
	require.NoError(t, err)
	sub, err := cm.Sub(name)
	require.NoError(t, err)
	sub, err = sub.Sub("resource_attributes")
	require.NoError(t, err)
	cfg := DefaultResourceAttributesConfig()
	require.NoError(t, sub.Unmarshal(&cfg))
	return cfg
}
{{- end }}
