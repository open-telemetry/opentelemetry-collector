# Use this changelog template to create an entry for release notes.

# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: bug_fix

# The name of the component, or a single word describing the area of concern, (e.g. receiver/otlp)
component: pkg/service

# A brief description of the change.  Surround your text with quotes ("") if it needs to start with a backtick (`).
note: Fix deadlock when components report fatal errors during startup, reload, or shutdown

# One or more tracking issues or pull requests related to the change
issues: [9824]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use pipe (|) for multiline entries.
subtext: |
  The collector's control loop only reads from the asyncErrorChannel while it is
  actively selecting, so a component reporting a fatal error during startup,
  configuration reload, or shutdown blocked forever on the channel send. A
  dedicated drain goroutine now services the channel for the lifetime of Run,
  forwarding the first fatal error to the control loop and coalescing further
  errors. The send in NotifyComponentStatusChange is also non-blocking, so
  reporters can never deadlock even outside the drain's lifetime.

# Optional: The change log or logs in which this entry should be included.
# e.g. '[user]' or '[user, api]'
# Include 'user' if the change is relevant to end users.
# Include 'api' if there is a change to a library API.
# Default: '[user]'
change_logs: [user]
