change_type: enhancement

component: processor/queuebatch

note: Report queue and batch telemetry using processor-oriented metrics

issues: [14038]

subtext: |
  Exporterhelper now provides a sealed, signal-scoped metrics interface,
  allowing queuebatchprocessor to connect its generated `otelcol_processor_*`
  instruments without rewriting exporter telemetry.

change_logs: [user, api]
