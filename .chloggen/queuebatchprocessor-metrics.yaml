change_type: enhancement

component: processor/queuebatch

note: Report queue and batch telemetry using processor-oriented metrics

issues: [14038]

subtext: |
  The processor reports `otelcol_processor_*` instruments with `processor` and
  `otel.signal` attributes instead of exporterhelper's `otelcol_exporter_*`
  instruments. It does not emit any `otelcol_exporter_*` metric.

change_logs: [user, api]
