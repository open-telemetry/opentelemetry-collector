change_type: enhancement

component: processor/queuebatch

note: Report queue and batch telemetry using processor-oriented metrics

issues: [14038]

subtext: |
  Exporterhelper now accepts a signal-scoped metrics implementation, allowing
  queuebatchprocessor to emit `otelcol_processor_*` metrics with a `processor`
  attribute without rewriting exporter telemetry.

change_logs: [user, api]
