receivers:
  examplereceiver:
processors:
  exampleprocessor:
exporters:
  exampleexporter:

pipelines:
  wrongdatatype:
    receivers: [examplereceiver]
    processors: [exampleprocessor]
    exporters: [exampleexporter]
