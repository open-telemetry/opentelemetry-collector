receivers:
  examplereceiver:
processors:
  exampleprocessor:
exporters:
  exampleexporter:

pipelines:
  /metrics:
    receivers: [examplereceiver]
    processors: [exampleprocessor]
    exporters: [exampleexporter]
