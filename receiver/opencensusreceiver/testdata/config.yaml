receivers:
  opentelemetry:
  opentelemetry/customname:
    endpoint: 0.0.0.0:9090

processors:
  exampleprocessor:

exporters:
  exampleexporter:

pipelines:
  traces:
      receivers: [opentelemetry]
      processors: [exampleprocessor]
      exporters: [exampleexporter]

