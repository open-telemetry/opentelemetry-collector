receivers:
  opentelemtry:
  opentelemtry/customname:
    endpoint: 0.0.0.0:9090

processors:
  exampleprocessor:

exporters:
  exampleexporter:

pipelines:
  traces:
      receivers: [opentelemtry]
      processors: [exampleprocessor]
      exporters: [exampleexporter]

