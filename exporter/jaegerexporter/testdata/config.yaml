receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  jaeger:
  jaeger/2:
    enabled: true
    CollectorEndpoint: "1.2.3.4:1234"
    Username: "user"
    Password: "j12345"
    num-workers: 123

pipelines:
  traces:
    receivers: [examplereceiver]
    processors: [exampleprocessor]
    exporters: [jaeger]
