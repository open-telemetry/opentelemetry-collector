receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  jaeger:
  jaeger/2:
    enabled: true
    collector-endpoint: "1.2.3.4:1234"
    username: "user"
    password: "j12345"
    service-name: "trace-test"
pipelines:
  traces:
    receivers: [examplereceiver]
    processors: [exampleprocessor]
    exporters: [jaeger]
