receivers:
  examplereceiver:

exporters:
  exampleexporter:

processors:
  aggregate:
    peer_discovery_dns_name: "otel-agent.my-ns.svc.cluster.local"


service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [aggregate]
      exporters: [exampleexporter]
