receivers:
  examplereceiver:

processors:
  exprfilter:
    exclude:
      - "MetricName == 'foo'"

exporters:
  exampleexporter:

service:
  pipelines:
    metrics:
      receivers: [examplereceiver]
      processors: [exprfilter]
      exporters: [exampleexporter]
