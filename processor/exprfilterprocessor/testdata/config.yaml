receivers:
  examplereceiver:

processors:
  exprfilter:

exporters:
  exampleexporter:

service:
  pipelines:
    metrics:
      receivers: [examplereceiver]
      processors: [exprfilter]
      exporters: [exampleexporter]
