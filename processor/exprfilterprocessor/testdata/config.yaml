receivers:
  examplereceiver:

processors:
  exprfilter:
    query:
      - "MetricName == 'foo'"

exporters:
  exampleexporter:

service:
  pipelines:
    metrics:
      receivers: [examplereceiver]
      processors: [exprfilter]
      exporters: [exampleexporter]
