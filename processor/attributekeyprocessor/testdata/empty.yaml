receivers:
  examplereceiver:

processors:
  attributes:
    

exporters:
  exampleexporter:

pipelines:
  traces:
    receivers: [examplereceiver]
    processors: [attributes]
    exporters: [exampleexporter]
