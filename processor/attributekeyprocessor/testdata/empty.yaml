receivers:
  examplereceiver:

processors:
  attribute-key:
    

exporters:
  exampleexporter:

pipelines:
  traces:
    receivers: [examplereceiver]
    processors: [attribute-key]
    exporters: [exampleexporter]
