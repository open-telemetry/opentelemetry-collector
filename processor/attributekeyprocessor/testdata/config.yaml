receivers:
  examplereceiver:

processors:
  attributes:
    key-replacements:
      foo: 
          replacement: "boo"
          overwrite: true
          keep: true
      kie: 
          replacement: "goo"
          overwrite: true
          keep: false
      ddd:
          replacement: "vss"
          overwrite: false
          keep: true    

exporters:
  exampleexporter:

pipelines:
  traces:
    receivers: [examplereceiver]
    processors: [attributes]
    exporters: [exampleexporter]
