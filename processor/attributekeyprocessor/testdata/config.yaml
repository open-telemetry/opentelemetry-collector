receivers:
  examplereceiver:

processors:
  attribute-key:
    key-replacements:
      - 
        key: "foo"
        replacement: "boo"
        overwrite: true
        keep: true
      - 
        key: "kie"
        replacement: "goo"
        overwrite: true
        keep: false
      - 
        key: "ddd"
        replacement: "vss"
        overwrite: false
        keep: true    

exporters:
  exampleexporter:

pipelines:
  traces:
    receivers: [examplereceiver]
    processors: [attribute-key]
    exporters: [exampleexporter]
