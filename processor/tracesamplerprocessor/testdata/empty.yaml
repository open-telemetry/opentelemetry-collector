receivers:
  examplereceiver:

processors:
  trace-sample:
    

exporters:
  exampleexporter:

pipelines:
  traces:
    receivers: [examplereceiver]
    processors: [trace-sample]
    exporters: [exampleexporter]
