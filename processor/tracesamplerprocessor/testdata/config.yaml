receivers:
  examplereceiver:

processors:
  trace-sample:
    sampling-percentage: 15.3
    hash-seed: 22

exporters:
  exampleexporter:

pipelines:
  traces:
    receivers: [examplereceiver]
    processors: [trace-sample]
    exporters: [exampleexporter]
