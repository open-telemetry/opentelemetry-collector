receivers:
  examplereceiver:

processors:
  telemetry: {}

exporters:
  exampleexporter:

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [telemetry]
      exporters: [exampleexporter]
