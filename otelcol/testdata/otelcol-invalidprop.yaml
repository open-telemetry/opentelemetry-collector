receivers:
  nop:

processors:
  nop:

exporters:
  nop:

service:
  telemetry:
    traces:
      enabled: true
      propagators:
      - "unknown"
      - "tracecontext"
    metrics:
      address: localhost:8888
    logs:
      enabled: false
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
