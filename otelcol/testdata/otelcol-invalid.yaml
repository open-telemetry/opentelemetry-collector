receivers:
  nop:

processors:
  nop:

exporters:
  nop:

service:
  telemetry:
    metrics:
      address: localhost:8888
    logs:
      disabled: true
  pipelines:
    traces:
      receivers: [nop]
      processors: [invalid]
      exporters: [nop]
