receivers:
  nop:

processors:
  nop:

exporters:
  nop:

service:
  telemetry:
    metrics:
      address: localhost:8888
    logs:
      enabled: false
  pipelines:
    traces:
      receivers: [nop]
      processors: [invalid]
      exporters: [nop]
