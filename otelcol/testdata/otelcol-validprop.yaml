receivers:
  nop:

processors:
  nop:

exporters:
  nop:

service:
  telemetry:
    traces:
      propagators:
        - "b3"
        - "tracecontext"
    metrics:
      address: localhost:8888
    logs:
      enabled: false
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
