receivers:
  nop:

processors:
  nop:

exporters:
  nop:

pipelines:
  traces:
    receivers: [nop]
    processors: [invalid]
    exporters: [nop]
  traces:
    receivers: [nop]
    processors: [invalid]
    exporters: [nop]
