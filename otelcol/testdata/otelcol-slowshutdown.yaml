receivers:
  nop:

processors:
  nop:

exporters:
  nop:

extensions:
  slowshutdown:

service:
  extensions: [slowshutdown]
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]
