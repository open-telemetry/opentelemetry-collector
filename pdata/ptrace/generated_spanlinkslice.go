// Copyright The OpenTelemetry Authors
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//      http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Code generated by "pdata/internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package ptrace

import (
	"sort"

	otlptrace "go.opentelemetry.io/collector/pdata/internal/data/protogen/trace/v1"
)

// SpanLinkSlice logically represents a slice of SpanLink.
type SpanLinkSlice interface {
	commonSpanLinkSlice
	At(ix int) SpanLink
}

type MutableSpanLinkSlice interface {
	commonSpanLinkSlice
	At(ix int) MutableSpanLink
	EnsureCapacity(newCap int)
	AppendEmpty() MutableSpanLink
	Sort(less func(a, b MutableSpanLink) bool)
}

type commonSpanLinkSlice interface {
	Len() int
	CopyTo(dest MutableSpanLinkSlice)
	getOrig() *[]*otlptrace.Span_Link
}

type immutableSpanLinkSlice struct {
	orig *[]*otlptrace.Span_Link
}

type mutableSpanLinkSlice struct {
	immutableSpanLinkSlice
}

func (es immutableSpanLinkSlice) getOrig() *[]*otlptrace.Span_Link {
	return es.orig
}

func newImmutableSpanLinkSlice(orig *[]*otlptrace.Span_Link) immutableSpanLinkSlice {
	return immutableSpanLinkSlice{orig}
}

func newMutableSpanLinkSlice(orig *[]*otlptrace.Span_Link) mutableSpanLinkSlice {
	return mutableSpanLinkSlice{immutableSpanLinkSlice{orig}}
}

// NewSpanLinkSlice creates a SpanLinkSlice with 0 elements.
// Can use "EnsureCapacity" to initialize with a given capacity.
func NewSpanLinkSlice() MutableSpanLinkSlice {
	orig := []*otlptrace.Span_Link(nil)
	return newMutableSpanLinkSlice(&orig)
}

// Len returns the number of elements in the slice.
//
// Returns "0" for a newly instance created with "NewSpanLinkSlice()".
func (es immutableSpanLinkSlice) Len() int {
	return len(*es.getOrig())
}

// At returns the element at the given index.
//
// This function is used mostly for iterating over all the values in the slice:
//
//	for i := 0; i < es.Len(); i++ {
//	    e := es.At(i)
//	    ... // Do something with the element
//	}
func (es immutableSpanLinkSlice) At(ix int) SpanLink {
	return newImmutableSpanLink((*es.getOrig())[ix])
}

func (es mutableSpanLinkSlice) At(ix int) MutableSpanLink {
	return newMutableSpanLink((*es.getOrig())[ix])
}

// CopyTo copies all elements from the current slice overriding the destination.
func (es immutableSpanLinkSlice) CopyTo(dest MutableSpanLinkSlice) {
	srcLen := es.Len()
	destCap := cap(*dest.getOrig())
	if srcLen <= destCap {
		(*dest.getOrig()) = (*dest.getOrig())[:srcLen:destCap]
		for i := range *es.getOrig() {
			newImmutableSpanLink((*es.getOrig())[i]).CopyTo(newMutableSpanLink((*dest.getOrig())[i]))
		}
		return
	}
	origs := make([]otlptrace.Span_Link, srcLen)
	wrappers := make([]*otlptrace.Span_Link, srcLen)
	for i := range *es.getOrig() {
		wrappers[i] = &origs[i]
		newImmutableSpanLink((*es.getOrig())[i]).CopyTo(newMutableSpanLink(wrappers[i]))
	}
	*dest.getOrig() = wrappers
}

// EnsureCapacity is an operation that ensures the slice has at least the specified capacity.
// 1. If the newCap <= cap then no change in capacity.
// 2. If the newCap > cap then the slice capacity will be expanded to equal newCap.
//
// Here is how a new SpanLinkSlice can be initialized:
//
//	es := NewSpanLinkSlice()
//	es.EnsureCapacity(4)
//	for i := 0; i < 4; i++ {
//	    e := es.AppendEmpty()
//	    // Here should set all the values for e.
//	}
func (es mutableSpanLinkSlice) EnsureCapacity(newCap int) {
	oldCap := cap(*es.getOrig())
	if newCap <= oldCap {
		return
	}

	newOrig := make([]*otlptrace.Span_Link, len(*es.getOrig()), newCap)
	copy(newOrig, *es.getOrig())
	*es.getOrig() = newOrig
}

// AppendEmpty will append to the end of the slice an empty SpanLink.
// It returns the newly added SpanLink.
func (es mutableSpanLinkSlice) AppendEmpty() MutableSpanLink {
	*es.getOrig() = append(*es.getOrig(), &otlptrace.Span_Link{})
	return es.At(es.Len() - 1)
}

// Sort sorts the SpanLink elements within SpanLinkSlice given the
// provided less function so that two instances of SpanLinkSlice
// can be compared.
func (es mutableSpanLinkSlice) Sort(less func(a, b MutableSpanLink) bool) {
	sort.SliceStable(*es.getOrig(), func(i, j int) bool { return less(es.At(i), es.At(j)) })
}

// MoveAndAppendTo moves all elements from the current slice and appends them to the dest.
// The current slice will be cleared.
func (es mutableSpanLinkSlice) MoveAndAppendTo(dest mutableSpanLinkSlice) {
	if *dest.getOrig() == nil {
		// We can simply move the entire vector and avoid any allocations.
		*dest.getOrig() = *es.getOrig()
	} else {
		*dest.getOrig() = append(*dest.getOrig(), *es.getOrig()...)
	}
	*es.getOrig() = nil
}

// RemoveIf calls f sequentially for each element present in the slice.
// If f returns true, the element is removed from the slice.
func (es mutableSpanLinkSlice) RemoveIf(f func(MutableSpanLink) bool) {
	newLen := 0
	for i := 0; i < len(*es.getOrig()); i++ {
		if f(es.At(i)) {
			continue
		}
		if newLen == i {
			// Nothing to move, element is at the right place.
			newLen++
			continue
		}
		(*es.getOrig())[newLen] = (*es.getOrig())[i]
		newLen++
	}
	// TODO: Prevent memory leak by erasing truncated values.
	*es.orig = (*es.orig)[:newLen]
}

func generateTestSpanLinkSlice() MutableSpanLinkSlice {
	tv := NewSpanLinkSlice()
	fillTestSpanLinkSlice(tv)
	return tv
}

func fillTestSpanLinkSlice(tv MutableSpanLinkSlice) {
	*tv.orig = make([]*otlptrace.Span_Link, 7)
	for i := 0; i < 7; i++ {
		(*tv.orig)[i] = &otlptrace.Span_Link{}
		fillTestSpanLink(newSpanLink((*tv.orig)[i]))
	}
}
