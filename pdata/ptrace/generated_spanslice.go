// Copyright The OpenTelemetry Authors
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//      http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Code generated by "pdata/internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package ptrace

import (
	"sort"

	otlptrace "go.opentelemetry.io/collector/pdata/internal/data/protogen/trace/v1"
)

// SpanSlice logically represents a slice of Span.
type SpanSlice interface {
	commonSpanSlice
	At(ix int) Span
}

type MutableSpanSlice interface {
	commonSpanSlice
	At(ix int) MutableSpan
	EnsureCapacity(newCap int)
	AppendEmpty() MutableSpan
	Sort(less func(a, b MutableSpan) bool)
}

type commonSpanSlice interface {
	Len() int
	CopyTo(dest MutableSpanSlice)
	getOrig() *[]*otlptrace.Span
}

type immutableSpanSlice struct {
	orig *[]*otlptrace.Span
}

type mutableSpanSlice struct {
	immutableSpanSlice
}

func (es immutableSpanSlice) getOrig() *[]*otlptrace.Span {
	return es.orig
}

func newImmutableSpanSlice(orig *[]*otlptrace.Span) immutableSpanSlice {
	return immutableSpanSlice{orig}
}

func newMutableSpanSlice(orig *[]*otlptrace.Span) mutableSpanSlice {
	return mutableSpanSlice{immutableSpanSlice{orig}}
}

// NewSpanSlice creates a SpanSlice with 0 elements.
// Can use "EnsureCapacity" to initialize with a given capacity.
func NewSpanSlice() MutableSpanSlice {
	orig := []*otlptrace.Span(nil)
	return newMutableSpanSlice(&orig)
}

// Len returns the number of elements in the slice.
//
// Returns "0" for a newly instance created with "NewSpanSlice()".
func (es immutableSpanSlice) Len() int {
	return len(*es.getOrig())
}

// At returns the element at the given index.
//
// This function is used mostly for iterating over all the values in the slice:
//
//	for i := 0; i < es.Len(); i++ {
//	    e := es.At(i)
//	    ... // Do something with the element
//	}
func (es immutableSpanSlice) At(ix int) Span {
	return newImmutableSpan((*es.getOrig())[ix])
}

func (es mutableSpanSlice) At(ix int) MutableSpan {
	return newMutableSpan((*es.getOrig())[ix])
}

// CopyTo copies all elements from the current slice overriding the destination.
func (es immutableSpanSlice) CopyTo(dest MutableSpanSlice) {
	srcLen := es.Len()
	destCap := cap(*dest.getOrig())
	if srcLen <= destCap {
		(*dest.getOrig()) = (*dest.getOrig())[:srcLen:destCap]
		for i := range *es.getOrig() {
			newImmutableSpan((*es.getOrig())[i]).CopyTo(newMutableSpan((*dest.getOrig())[i]))
		}
		return
	}
	origs := make([]otlptrace.Span, srcLen)
	wrappers := make([]*otlptrace.Span, srcLen)
	for i := range *es.getOrig() {
		wrappers[i] = &origs[i]
		newImmutableSpan((*es.getOrig())[i]).CopyTo(newMutableSpan(wrappers[i]))
	}
	*dest.getOrig() = wrappers
}

// EnsureCapacity is an operation that ensures the slice has at least the specified capacity.
// 1. If the newCap <= cap then no change in capacity.
// 2. If the newCap > cap then the slice capacity will be expanded to equal newCap.
//
// Here is how a new SpanSlice can be initialized:
//
//	es := NewSpanSlice()
//	es.EnsureCapacity(4)
//	for i := 0; i < 4; i++ {
//	    e := es.AppendEmpty()
//	    // Here should set all the values for e.
//	}
func (es mutableSpanSlice) EnsureCapacity(newCap int) {
	oldCap := cap(*es.getOrig())
	if newCap <= oldCap {
		return
	}

	newOrig := make([]*otlptrace.Span, len(*es.getOrig()), newCap)
	copy(newOrig, *es.getOrig())
	*es.getOrig() = newOrig
}

// AppendEmpty will append to the end of the slice an empty Span.
// It returns the newly added Span.
func (es mutableSpanSlice) AppendEmpty() MutableSpan {
	*es.getOrig() = append(*es.getOrig(), &otlptrace.Span{})
	return es.At(es.Len() - 1)
}

// Sort sorts the Span elements within SpanSlice given the
// provided less function so that two instances of SpanSlice
// can be compared.
func (es mutableSpanSlice) Sort(less func(a, b MutableSpan) bool) {
	sort.SliceStable(*es.getOrig(), func(i, j int) bool { return less(es.At(i), es.At(j)) })
}

// MoveAndAppendTo moves all elements from the current slice and appends them to the dest.
// The current slice will be cleared.
func (es mutableSpanSlice) MoveAndAppendTo(dest mutableSpanSlice) {
	if *dest.getOrig() == nil {
		// We can simply move the entire vector and avoid any allocations.
		*dest.getOrig() = *es.getOrig()
	} else {
		*dest.getOrig() = append(*dest.getOrig(), *es.getOrig()...)
	}
	*es.getOrig() = nil
}

// RemoveIf calls f sequentially for each element present in the slice.
// If f returns true, the element is removed from the slice.
func (es mutableSpanSlice) RemoveIf(f func(MutableSpan) bool) {
	newLen := 0
	for i := 0; i < len(*es.getOrig()); i++ {
		if f(es.At(i)) {
			continue
		}
		if newLen == i {
			// Nothing to move, element is at the right place.
			newLen++
			continue
		}
		(*es.getOrig())[newLen] = (*es.getOrig())[i]
		newLen++
	}
	// TODO: Prevent memory leak by erasing truncated values.
	*es.orig = (*es.orig)[:newLen]
}

func generateTestSpanSlice() MutableSpanSlice {
	tv := NewSpanSlice()
	fillTestSpanSlice(tv)
	return tv
}

func fillTestSpanSlice(tv MutableSpanSlice) {
	*tv.orig = make([]*otlptrace.Span, 7)
	for i := 0; i < 7; i++ {
		(*tv.orig)[i] = &otlptrace.Span{}
		fillTestSpan(newSpan((*tv.orig)[i]))
	}
}
