// Copyright The OpenTelemetry Authors
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//      http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Code generated by "pdata/internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package pmetric

import (
	"sort"

	otlpmetrics "go.opentelemetry.io/collector/pdata/internal/data/protogen/metrics/v1"
)

// NumberDataPointSlice logically represents a slice of NumberDataPoint.
type NumberDataPointSlice interface {
	commonNumberDataPointSlice
	At(ix int) NumberDataPoint
}

type MutableNumberDataPointSlice interface {
	commonNumberDataPointSlice
	At(ix int) MutableNumberDataPoint
	EnsureCapacity(newCap int)
	AppendEmpty() MutableNumberDataPoint
	Sort(less func(a, b MutableNumberDataPoint) bool)
}

type commonNumberDataPointSlice interface {
	Len() int
	CopyTo(dest MutableNumberDataPointSlice)
	getOrig() *[]*otlpmetrics.NumberDataPoint
}

type immutableNumberDataPointSlice struct {
	orig *[]*otlpmetrics.NumberDataPoint
}

type mutableNumberDataPointSlice struct {
	immutableNumberDataPointSlice
}

func (es immutableNumberDataPointSlice) getOrig() *[]*otlpmetrics.NumberDataPoint {
	return es.orig
}

func newImmutableNumberDataPointSlice(orig *[]*otlpmetrics.NumberDataPoint) immutableNumberDataPointSlice {
	return immutableNumberDataPointSlice{orig}
}

func newMutableNumberDataPointSlice(orig *[]*otlpmetrics.NumberDataPoint) mutableNumberDataPointSlice {
	return mutableNumberDataPointSlice{immutableNumberDataPointSlice{orig}}
}

// NewNumberDataPointSlice creates a NumberDataPointSlice with 0 elements.
// Can use "EnsureCapacity" to initialize with a given capacity.
func NewNumberDataPointSlice() MutableNumberDataPointSlice {
	orig := []*otlpmetrics.NumberDataPoint(nil)
	return newMutableNumberDataPointSlice(&orig)
}

// Len returns the number of elements in the slice.
//
// Returns "0" for a newly instance created with "NewNumberDataPointSlice()".
func (es immutableNumberDataPointSlice) Len() int {
	return len(*es.getOrig())
}

// At returns the element at the given index.
//
// This function is used mostly for iterating over all the values in the slice:
//
//	for i := 0; i < es.Len(); i++ {
//	    e := es.At(i)
//	    ... // Do something with the element
//	}
func (es immutableNumberDataPointSlice) At(ix int) NumberDataPoint {
	return newImmutableNumberDataPoint((*es.getOrig())[ix])
}

func (es mutableNumberDataPointSlice) At(ix int) MutableNumberDataPoint {
	return newMutableNumberDataPoint((*es.getOrig())[ix])
}

// CopyTo copies all elements from the current slice overriding the destination.
func (es immutableNumberDataPointSlice) CopyTo(dest MutableNumberDataPointSlice) {
	srcLen := es.Len()
	destCap := cap(*dest.getOrig())
	if srcLen <= destCap {
		(*dest.getOrig()) = (*dest.getOrig())[:srcLen:destCap]
		for i := range *es.getOrig() {
			newImmutableNumberDataPoint((*es.getOrig())[i]).CopyTo(newMutableNumberDataPoint((*dest.getOrig())[i]))
		}
		return
	}
	origs := make([]otlpmetrics.NumberDataPoint, srcLen)
	wrappers := make([]*otlpmetrics.NumberDataPoint, srcLen)
	for i := range *es.getOrig() {
		wrappers[i] = &origs[i]
		newImmutableNumberDataPoint((*es.getOrig())[i]).CopyTo(newMutableNumberDataPoint(wrappers[i]))
	}
	*dest.getOrig() = wrappers
}

// EnsureCapacity is an operation that ensures the slice has at least the specified capacity.
// 1. If the newCap <= cap then no change in capacity.
// 2. If the newCap > cap then the slice capacity will be expanded to equal newCap.
//
// Here is how a new NumberDataPointSlice can be initialized:
//
//	es := NewNumberDataPointSlice()
//	es.EnsureCapacity(4)
//	for i := 0; i < 4; i++ {
//	    e := es.AppendEmpty()
//	    // Here should set all the values for e.
//	}
func (es mutableNumberDataPointSlice) EnsureCapacity(newCap int) {
	oldCap := cap(*es.getOrig())
	if newCap <= oldCap {
		return
	}

	newOrig := make([]*otlpmetrics.NumberDataPoint, len(*es.getOrig()), newCap)
	copy(newOrig, *es.getOrig())
	*es.getOrig() = newOrig
}

// AppendEmpty will append to the end of the slice an empty NumberDataPoint.
// It returns the newly added NumberDataPoint.
func (es mutableNumberDataPointSlice) AppendEmpty() MutableNumberDataPoint {
	*es.getOrig() = append(*es.getOrig(), &otlpmetrics.NumberDataPoint{})
	return es.At(es.Len() - 1)
}

// Sort sorts the NumberDataPoint elements within NumberDataPointSlice given the
// provided less function so that two instances of NumberDataPointSlice
// can be compared.
func (es mutableNumberDataPointSlice) Sort(less func(a, b MutableNumberDataPoint) bool) {
	sort.SliceStable(*es.getOrig(), func(i, j int) bool { return less(es.At(i), es.At(j)) })
}

// MoveAndAppendTo moves all elements from the current slice and appends them to the dest.
// The current slice will be cleared.
func (es mutableNumberDataPointSlice) MoveAndAppendTo(dest mutableNumberDataPointSlice) {
	if *dest.getOrig() == nil {
		// We can simply move the entire vector and avoid any allocations.
		*dest.getOrig() = *es.getOrig()
	} else {
		*dest.getOrig() = append(*dest.getOrig(), *es.getOrig()...)
	}
	*es.getOrig() = nil
}

// RemoveIf calls f sequentially for each element present in the slice.
// If f returns true, the element is removed from the slice.
func (es mutableNumberDataPointSlice) RemoveIf(f func(MutableNumberDataPoint) bool) {
	newLen := 0
	for i := 0; i < len(*es.getOrig()); i++ {
		if f(es.At(i)) {
			continue
		}
		if newLen == i {
			// Nothing to move, element is at the right place.
			newLen++
			continue
		}
		(*es.getOrig())[newLen] = (*es.getOrig())[i]
		newLen++
	}
	// TODO: Prevent memory leak by erasing truncated values.
	*es.orig = (*es.orig)[:newLen]
}

func generateTestNumberDataPointSlice() MutableNumberDataPointSlice {
	tv := NewNumberDataPointSlice()
	fillTestNumberDataPointSlice(tv)
	return tv
}

func fillTestNumberDataPointSlice(tv MutableNumberDataPointSlice) {
	*tv.orig = make([]*otlpmetrics.NumberDataPoint, 7)
	for i := 0; i < 7; i++ {
		(*tv.orig)[i] = &otlpmetrics.NumberDataPoint{}
		fillTestNumberDataPoint(newNumberDataPoint((*tv.orig)[i]))
	}
}
