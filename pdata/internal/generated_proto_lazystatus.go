// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// Status is an optional final status for this span. Semantically, when Status was not
// set, that means the span ended without errors and to assume Status.Ok (code = 0).
type LazyStatus struct {
	Status
	bytes []byte
}

var (
	protoPoolLazyStatus = sync.Pool{
		New: func() any {
			return &LazyStatus{}
		},
	}
)

func NewLazyStatus() *LazyStatus {
	if !UseProtoPooling.IsEnabled() {
		return &LazyStatus{}
	}
	return protoPoolLazyStatus.Get().(*LazyStatus)
}

func DeleteLazyStatus(orig *LazyStatus, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteStatus(&orig.Status, false)

	orig.Reset()
	if nullable {
		protoPoolLazyStatus.Put(orig)
	}
}

func CopyLazyStatus(dest, src *LazyStatus) *LazyStatus {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyStatus()
	}

	CopyStatus(&dest.Status, &src.Status)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyStatusSlice(dest, src []LazyStatus) []LazyStatus {
	var newDest []LazyStatus
	if cap(dest) < len(src) {
		newDest = make([]LazyStatus, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyStatus(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyStatus(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyStatusPtrSlice(dest, src []*LazyStatus) []*LazyStatus {
	var newDest []*LazyStatus
	if cap(dest) < len(src) {
		newDest = make([]*LazyStatus, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyStatus()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyStatus(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyStatus()
		}
	}
	for i := range src {
		CopyLazyStatus(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyStatus) Reset() {
	*orig = LazyStatus{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyStatus) MarshalJSON(dest *json.Stream) {
	orig.Status.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyStatus) UnmarshalJSON(iter *json.Iterator) {
	orig.Status.UnmarshalJSON(iter)
}

func (orig *LazyStatus) SizeProto() int {
	return orig.Status.SizeProto()
}

func (orig *LazyStatus) MarshalProto(buf []byte) int {
	return orig.Status.MarshalProto(buf)
}

func (orig *LazyStatus) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyStatus) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.Status.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.Status.Reset()
	return nil
}

func (orig *LazyStatus) FinishUnmarshal(buf *Status) (*Status, error) {
	if len(orig.bytes) == 0 {
		return &orig.Status, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyStatus() *LazyStatus {
	orig := NewLazyStatus()
	orig.Status = *GenTestStatus()
	return orig
}

func GenTestLazyStatusPtrSlice() []*LazyStatus {
	orig := make([]*LazyStatus, 5)
	orig[0] = NewLazyStatus()
	orig[1] = GenTestLazyStatus()
	orig[2] = NewLazyStatus()
	orig[3] = GenTestLazyStatus()
	orig[4] = NewLazyStatus()
	return orig
}

func GenTestLazyStatusSlice() []LazyStatus {
	orig := make([]LazyStatus, 5)
	orig[1] = *GenTestLazyStatus()
	orig[3] = *GenTestLazyStatus()
	return orig
}
