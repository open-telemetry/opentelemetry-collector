// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// Stack represents a stack trace as a list of locations.

type LazyStack struct {
	Stack
	bytes []byte
}

var (
	protoPoolLazyStack = sync.Pool{
		New: func() any {
			return &LazyStack{}
		},
	}
)

func NewLazyStack() *LazyStack {
	if !UseProtoPooling.IsEnabled() {
		return &LazyStack{}
	}
	return protoPoolLazyStack.Get().(*LazyStack)
}

func DeleteLazyStack(orig *LazyStack, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteStack(&orig.Stack, false)

	orig.Reset()
	if nullable {
		protoPoolLazyStack.Put(orig)
	}
}

func CopyLazyStack(dest, src *LazyStack) *LazyStack {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyStack()
	}

	CopyStack(&dest.Stack, &src.Stack)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyStackSlice(dest, src []LazyStack) []LazyStack {
	var newDest []LazyStack
	if cap(dest) < len(src) {
		newDest = make([]LazyStack, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyStack(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyStack(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyStackPtrSlice(dest, src []*LazyStack) []*LazyStack {
	var newDest []*LazyStack
	if cap(dest) < len(src) {
		newDest = make([]*LazyStack, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyStack()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyStack(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyStack()
		}
	}
	for i := range src {
		CopyLazyStack(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyStack) Reset() {
	*orig = LazyStack{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyStack) MarshalJSON(dest *json.Stream) {
	orig.Stack.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyStack) UnmarshalJSON(iter *json.Iterator) {
	orig.Stack.UnmarshalJSON(iter)
}

func (orig *LazyStack) SizeProto() int {
	return orig.Stack.SizeProto()
}

func (orig *LazyStack) MarshalProto(buf []byte) int {
	return orig.Stack.MarshalProto(buf)
}

func (orig *LazyStack) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyStack) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.Stack.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.Stack.Reset()
	return nil
}

func (orig *LazyStack) FinishUnmarshal(buf *Stack) (*Stack, error) {
	if len(orig.bytes) == 0 {
		return &orig.Stack, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyStack() *LazyStack {
	orig := NewLazyStack()
	orig.Stack = *GenTestStack()
	return orig
}

func GenTestLazyStackPtrSlice() []*LazyStack {
	orig := make([]*LazyStack, 5)
	orig[0] = NewLazyStack()
	orig[1] = GenTestLazyStack()
	orig[2] = NewLazyStack()
	orig[3] = GenTestLazyStack()
	orig[4] = NewLazyStack()
	return orig
}

func GenTestLazyStackSlice() []LazyStack {
	orig := make([]LazyStack, 5)
	orig[1] = *GenTestLazyStack()
	orig[3] = *GenTestLazyStack()
	return orig
}
