// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// LogRecord are experimental implementation of OpenTelemetry Log Data Model.

type LazyLogRecord struct {
	LogRecord
	bytes []byte
}

var (
	protoPoolLazyLogRecord = sync.Pool{
		New: func() any {
			return &LazyLogRecord{}
		},
	}
)

func NewLazyLogRecord() *LazyLogRecord {
	if !UseProtoPooling.IsEnabled() {
		return &LazyLogRecord{}
	}
	return protoPoolLazyLogRecord.Get().(*LazyLogRecord)
}

func DeleteLazyLogRecord(orig *LazyLogRecord, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteLogRecord(&orig.LogRecord, false)

	orig.Reset()
	if nullable {
		protoPoolLazyLogRecord.Put(orig)
	}
}

func CopyLazyLogRecord(dest, src *LazyLogRecord) *LazyLogRecord {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyLogRecord()
	}

	CopyLogRecord(&dest.LogRecord, &src.LogRecord)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyLogRecordSlice(dest, src []LazyLogRecord) []LazyLogRecord {
	var newDest []LazyLogRecord
	if cap(dest) < len(src) {
		newDest = make([]LazyLogRecord, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyLogRecord(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyLogRecord(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyLogRecordPtrSlice(dest, src []*LazyLogRecord) []*LazyLogRecord {
	var newDest []*LazyLogRecord
	if cap(dest) < len(src) {
		newDest = make([]*LazyLogRecord, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyLogRecord()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyLogRecord(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyLogRecord()
		}
	}
	for i := range src {
		CopyLazyLogRecord(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyLogRecord) Reset() {
	*orig = LazyLogRecord{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyLogRecord) MarshalJSON(dest *json.Stream) {
	orig.LogRecord.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyLogRecord) UnmarshalJSON(iter *json.Iterator) {
	orig.LogRecord.UnmarshalJSON(iter)
}

func (orig *LazyLogRecord) SizeProto() int {
	return orig.LogRecord.SizeProto()
}

func (orig *LazyLogRecord) MarshalProto(buf []byte) int {
	return orig.LogRecord.MarshalProto(buf)
}

func (orig *LazyLogRecord) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyLogRecord) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.LogRecord.UnmarshalProtoOpts(buf, opts)
	}
	if err := SkipLogRecordProto(buf); err != nil {
		return err
	}
	orig.bytes = buf
	orig.LogRecord.Reset()
	return nil
}

func SkipLazyLogRecordProto(buf []byte) error {
	return SkipLogRecordProto(buf)
}

func (orig *LazyLogRecord) FinishUnmarshal(buf *LogRecord) (*LogRecord, error) {
	if len(orig.bytes) == 0 {
		return &orig.LogRecord, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyLogRecord() *LazyLogRecord {
	orig := NewLazyLogRecord()
	orig.LogRecord = *GenTestLogRecord()
	return orig
}

func GenTestLazyLogRecordPtrSlice() []*LazyLogRecord {
	orig := make([]*LazyLogRecord, 5)
	orig[0] = NewLazyLogRecord()
	orig[1] = GenTestLazyLogRecord()
	orig[2] = NewLazyLogRecord()
	orig[3] = GenTestLazyLogRecord()
	orig[4] = NewLazyLogRecord()
	return orig
}

func GenTestLazyLogRecordSlice() []LazyLogRecord {
	orig := make([]LazyLogRecord, 5)
	orig[1] = *GenTestLazyLogRecord()
	orig[3] = *GenTestLazyLogRecord()
	return orig
}
