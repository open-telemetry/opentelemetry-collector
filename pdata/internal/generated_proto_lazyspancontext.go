// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

type LazySpanContext struct {
	SpanContext
	bytes []byte
}

var (
	protoPoolLazySpanContext = sync.Pool{
		New: func() any {
			return &LazySpanContext{}
		},
	}
)

func NewLazySpanContext() *LazySpanContext {
	if !UseProtoPooling.IsEnabled() {
		return &LazySpanContext{}
	}
	return protoPoolLazySpanContext.Get().(*LazySpanContext)
}

func DeleteLazySpanContext(orig *LazySpanContext, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteSpanContext(&orig.SpanContext, false)

	orig.Reset()
	if nullable {
		protoPoolLazySpanContext.Put(orig)
	}
}

func CopyLazySpanContext(dest, src *LazySpanContext) *LazySpanContext {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazySpanContext()
	}

	CopySpanContext(&dest.SpanContext, &src.SpanContext)
	dest.bytes = src.bytes

	return dest
}

func CopyLazySpanContextSlice(dest, src []LazySpanContext) []LazySpanContext {
	var newDest []LazySpanContext
	if cap(dest) < len(src) {
		newDest = make([]LazySpanContext, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazySpanContext(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazySpanContext(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazySpanContextPtrSlice(dest, src []*LazySpanContext) []*LazySpanContext {
	var newDest []*LazySpanContext
	if cap(dest) < len(src) {
		newDest = make([]*LazySpanContext, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazySpanContext()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazySpanContext(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazySpanContext()
		}
	}
	for i := range src {
		CopyLazySpanContext(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazySpanContext) Reset() {
	*orig = LazySpanContext{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazySpanContext) MarshalJSON(dest *json.Stream) {
	orig.SpanContext.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazySpanContext) UnmarshalJSON(iter *json.Iterator) {
	orig.SpanContext.UnmarshalJSON(iter)
}

func (orig *LazySpanContext) SizeProto() int {
	return orig.SpanContext.SizeProto()
}

func (orig *LazySpanContext) MarshalProto(buf []byte) int {
	return orig.SpanContext.MarshalProto(buf)
}

func (orig *LazySpanContext) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazySpanContext) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.SpanContext.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.SpanContext.Reset()
	return nil
}

func (orig *LazySpanContext) FinishUnmarshal(buf *SpanContext) (*SpanContext, error) {
	if len(orig.bytes) == 0 {
		return &orig.SpanContext, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazySpanContext() *LazySpanContext {
	orig := NewLazySpanContext()
	orig.SpanContext = *GenTestSpanContext()
	return orig
}

func GenTestLazySpanContextPtrSlice() []*LazySpanContext {
	orig := make([]*LazySpanContext, 5)
	orig[0] = NewLazySpanContext()
	orig[1] = GenTestLazySpanContext()
	orig[2] = NewLazySpanContext()
	orig[3] = GenTestLazySpanContext()
	orig[4] = NewLazySpanContext()
	return orig
}

func GenTestLazySpanContextSlice() []LazySpanContext {
	orig := make([]LazySpanContext, 5)
	orig[1] = *GenTestLazySpanContext()
	orig[3] = *GenTestLazySpanContext()
	return orig
}
