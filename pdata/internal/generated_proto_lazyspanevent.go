// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// SpanEvent is a time-stamped annotation of the span, consisting of user-supplied
// text description and key-value pairs. See OTLP for event definition.
type LazySpanEvent struct {
	bytes []byte
	SpanEvent
}

var (
	protoPoolLazySpanEvent = sync.Pool{
		New: func() any {
			return &LazySpanEvent{}
		},
	}
)

func NewLazySpanEvent() *LazySpanEvent {
	if !UseProtoPooling.IsEnabled() {
		return &LazySpanEvent{}
	}
	return protoPoolLazySpanEvent.Get().(*LazySpanEvent)
}

func DeleteLazySpanEvent(orig *LazySpanEvent, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteSpanEvent(&orig.SpanEvent, false)

	orig.Reset()
	if nullable {
		protoPoolLazySpanEvent.Put(orig)
	}
}

func CopyLazySpanEvent(dest, src *LazySpanEvent) *LazySpanEvent {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazySpanEvent()
	}

	CopySpanEvent(&dest.SpanEvent, &src.SpanEvent)
	dest.bytes = src.bytes

	return dest
}

func CopyLazySpanEventSlice(dest, src []LazySpanEvent) []LazySpanEvent {
	var newDest []LazySpanEvent
	if cap(dest) < len(src) {
		newDest = make([]LazySpanEvent, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazySpanEvent(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazySpanEvent(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazySpanEventPtrSlice(dest, src []*LazySpanEvent) []*LazySpanEvent {
	var newDest []*LazySpanEvent
	if cap(dest) < len(src) {
		newDest = make([]*LazySpanEvent, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazySpanEvent()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazySpanEvent(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazySpanEvent()
		}
	}
	for i := range src {
		CopyLazySpanEvent(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazySpanEvent) Reset() {
	*orig = LazySpanEvent{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazySpanEvent) MarshalJSON(dest *json.Stream) {
	orig.SpanEvent.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazySpanEvent) UnmarshalJSON(iter *json.Iterator) {
	orig.SpanEvent.UnmarshalJSON(iter)
}

func (orig *LazySpanEvent) SizeProto() int {
	return orig.SpanEvent.SizeProto()
}

func (orig *LazySpanEvent) MarshalProto(buf []byte) int {
	return orig.SpanEvent.MarshalProto(buf)
}

func (orig *LazySpanEvent) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazySpanEvent) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.SpanEvent.UnmarshalProtoOpts(buf, opts)
	}
	if err := SkipSpanEventProto(buf); err != nil {
		return err
	}
	orig.bytes = buf
	orig.SpanEvent.Reset()
	return nil
}

func SkipLazySpanEventProto(buf []byte) error {
	return SkipSpanEventProto(buf)
}

func (orig *LazySpanEvent) FinishUnmarshal(buf *SpanEvent) (*SpanEvent, error) {
	if len(orig.bytes) == 0 {
		return &orig.SpanEvent, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazySpanEvent() *LazySpanEvent {
	orig := NewLazySpanEvent()
	orig.SpanEvent = *GenTestSpanEvent()
	return orig
}

func GenTestLazySpanEventPtrSlice() []*LazySpanEvent {
	orig := make([]*LazySpanEvent, 5)
	orig[0] = NewLazySpanEvent()
	orig[1] = GenTestLazySpanEvent()
	orig[2] = NewLazySpanEvent()
	orig[3] = GenTestLazySpanEvent()
	orig[4] = NewLazySpanEvent()
	return orig
}

func GenTestLazySpanEventSlice() []LazySpanEvent {
	orig := make([]LazySpanEvent, 5)
	orig[1] = *GenTestLazySpanEvent()
	orig[3] = *GenTestLazySpanEvent()
	return orig
}
