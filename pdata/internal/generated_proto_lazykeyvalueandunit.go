// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// KeyValueAndUnit represents a custom 'dictionary native'
// style of encoding attributes which is more convenient
// for profiles than opentelemetry.proto.common.v1.KeyValue.
type LazyKeyValueAndUnit struct {
	KeyValueAndUnit
	bytes []byte
}

var (
	protoPoolLazyKeyValueAndUnit = sync.Pool{
		New: func() any {
			return &LazyKeyValueAndUnit{}
		},
	}
)

func NewLazyKeyValueAndUnit() *LazyKeyValueAndUnit {
	if !UseProtoPooling.IsEnabled() {
		return &LazyKeyValueAndUnit{}
	}
	return protoPoolLazyKeyValueAndUnit.Get().(*LazyKeyValueAndUnit)
}

func DeleteLazyKeyValueAndUnit(orig *LazyKeyValueAndUnit, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteKeyValueAndUnit(&orig.KeyValueAndUnit, false)

	orig.Reset()
	if nullable {
		protoPoolLazyKeyValueAndUnit.Put(orig)
	}
}

func CopyLazyKeyValueAndUnit(dest, src *LazyKeyValueAndUnit) *LazyKeyValueAndUnit {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyKeyValueAndUnit()
	}

	CopyKeyValueAndUnit(&dest.KeyValueAndUnit, &src.KeyValueAndUnit)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyKeyValueAndUnitSlice(dest, src []LazyKeyValueAndUnit) []LazyKeyValueAndUnit {
	var newDest []LazyKeyValueAndUnit
	if cap(dest) < len(src) {
		newDest = make([]LazyKeyValueAndUnit, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyKeyValueAndUnit(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyKeyValueAndUnit(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyKeyValueAndUnitPtrSlice(dest, src []*LazyKeyValueAndUnit) []*LazyKeyValueAndUnit {
	var newDest []*LazyKeyValueAndUnit
	if cap(dest) < len(src) {
		newDest = make([]*LazyKeyValueAndUnit, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyKeyValueAndUnit()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyKeyValueAndUnit(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyKeyValueAndUnit()
		}
	}
	for i := range src {
		CopyLazyKeyValueAndUnit(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyKeyValueAndUnit) Reset() {
	*orig = LazyKeyValueAndUnit{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyKeyValueAndUnit) MarshalJSON(dest *json.Stream) {
	orig.KeyValueAndUnit.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyKeyValueAndUnit) UnmarshalJSON(iter *json.Iterator) {
	orig.KeyValueAndUnit.UnmarshalJSON(iter)
}

func (orig *LazyKeyValueAndUnit) SizeProto() int {
	return orig.KeyValueAndUnit.SizeProto()
}

func (orig *LazyKeyValueAndUnit) MarshalProto(buf []byte) int {
	return orig.KeyValueAndUnit.MarshalProto(buf)
}

func (orig *LazyKeyValueAndUnit) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyKeyValueAndUnit) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.KeyValueAndUnit.UnmarshalProtoOpts(buf, opts)
	}
	if err := SkipKeyValueAndUnitProto(buf); err != nil {
		return err
	}
	orig.bytes = buf
	orig.KeyValueAndUnit.Reset()
	return nil
}

func SkipLazyKeyValueAndUnitProto(buf []byte) error {
	return SkipKeyValueAndUnitProto(buf)
}

func (orig *LazyKeyValueAndUnit) FinishUnmarshal(buf *KeyValueAndUnit) (*KeyValueAndUnit, error) {
	if len(orig.bytes) == 0 {
		return &orig.KeyValueAndUnit, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyKeyValueAndUnit() *LazyKeyValueAndUnit {
	orig := NewLazyKeyValueAndUnit()
	orig.KeyValueAndUnit = *GenTestKeyValueAndUnit()
	return orig
}

func GenTestLazyKeyValueAndUnitPtrSlice() []*LazyKeyValueAndUnit {
	orig := make([]*LazyKeyValueAndUnit, 5)
	orig[0] = NewLazyKeyValueAndUnit()
	orig[1] = GenTestLazyKeyValueAndUnit()
	orig[2] = NewLazyKeyValueAndUnit()
	orig[3] = GenTestLazyKeyValueAndUnit()
	orig[4] = NewLazyKeyValueAndUnit()
	return orig
}

func GenTestLazyKeyValueAndUnitSlice() []LazyKeyValueAndUnit {
	orig := make([]LazyKeyValueAndUnit, 5)
	orig[1] = *GenTestLazyKeyValueAndUnit()
	orig[3] = *GenTestLazyKeyValueAndUnit()
	return orig
}
