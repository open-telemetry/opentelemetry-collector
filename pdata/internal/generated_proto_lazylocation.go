// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// Location describes function and line table debug information.
type LazyLocation struct {
	Location
	bytes []byte
}

var (
	protoPoolLazyLocation = sync.Pool{
		New: func() any {
			return &LazyLocation{}
		},
	}
)

func NewLazyLocation() *LazyLocation {
	if !UseProtoPooling.IsEnabled() {
		return &LazyLocation{}
	}
	return protoPoolLazyLocation.Get().(*LazyLocation)
}

func DeleteLazyLocation(orig *LazyLocation, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteLocation(&orig.Location, false)

	orig.Reset()
	if nullable {
		protoPoolLazyLocation.Put(orig)
	}
}

func CopyLazyLocation(dest, src *LazyLocation) *LazyLocation {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyLocation()
	}

	CopyLocation(&dest.Location, &src.Location)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyLocationSlice(dest, src []LazyLocation) []LazyLocation {
	var newDest []LazyLocation
	if cap(dest) < len(src) {
		newDest = make([]LazyLocation, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyLocation(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyLocation(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyLocationPtrSlice(dest, src []*LazyLocation) []*LazyLocation {
	var newDest []*LazyLocation
	if cap(dest) < len(src) {
		newDest = make([]*LazyLocation, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyLocation()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyLocation(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyLocation()
		}
	}
	for i := range src {
		CopyLazyLocation(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyLocation) Reset() {
	*orig = LazyLocation{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyLocation) MarshalJSON(dest *json.Stream) {
	orig.Location.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyLocation) UnmarshalJSON(iter *json.Iterator) {
	orig.Location.UnmarshalJSON(iter)
}

func (orig *LazyLocation) SizeProto() int {
	return orig.Location.SizeProto()
}

func (orig *LazyLocation) MarshalProto(buf []byte) int {
	return orig.Location.MarshalProto(buf)
}

func (orig *LazyLocation) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyLocation) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.Location.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.Location.Reset()
	return nil
}

func (orig *LazyLocation) FinishUnmarshal(buf *Location) (*Location, error) {
	if len(orig.bytes) == 0 {
		return &orig.Location, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyLocation() *LazyLocation {
	orig := NewLazyLocation()
	orig.Location = *GenTestLocation()
	return orig
}

func GenTestLazyLocationPtrSlice() []*LazyLocation {
	orig := make([]*LazyLocation, 5)
	orig[0] = NewLazyLocation()
	orig[1] = GenTestLazyLocation()
	orig[2] = NewLazyLocation()
	orig[3] = GenTestLazyLocation()
	orig[4] = NewLazyLocation()
	return orig
}

func GenTestLazyLocationSlice() []LazyLocation {
	orig := make([]LazyLocation, 5)
	orig[1] = *GenTestLazyLocation()
	orig[3] = *GenTestLazyLocation()
	return orig
}
