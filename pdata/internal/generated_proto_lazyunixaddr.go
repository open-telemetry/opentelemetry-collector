// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

type LazyUnixAddr struct {
	UnixAddr
	bytes []byte
}

var (
	protoPoolLazyUnixAddr = sync.Pool{
		New: func() any {
			return &LazyUnixAddr{}
		},
	}
)

func NewLazyUnixAddr() *LazyUnixAddr {
	if !UseProtoPooling.IsEnabled() {
		return &LazyUnixAddr{}
	}
	return protoPoolLazyUnixAddr.Get().(*LazyUnixAddr)
}

func DeleteLazyUnixAddr(orig *LazyUnixAddr, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteUnixAddr(&orig.UnixAddr, false)

	orig.Reset()
	if nullable {
		protoPoolLazyUnixAddr.Put(orig)
	}
}

func CopyLazyUnixAddr(dest, src *LazyUnixAddr) *LazyUnixAddr {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyUnixAddr()
	}

	CopyUnixAddr(&dest.UnixAddr, &src.UnixAddr)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyUnixAddrSlice(dest, src []LazyUnixAddr) []LazyUnixAddr {
	var newDest []LazyUnixAddr
	if cap(dest) < len(src) {
		newDest = make([]LazyUnixAddr, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyUnixAddr(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyUnixAddr(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyUnixAddrPtrSlice(dest, src []*LazyUnixAddr) []*LazyUnixAddr {
	var newDest []*LazyUnixAddr
	if cap(dest) < len(src) {
		newDest = make([]*LazyUnixAddr, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyUnixAddr()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyUnixAddr(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyUnixAddr()
		}
	}
	for i := range src {
		CopyLazyUnixAddr(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyUnixAddr) Reset() {
	*orig = LazyUnixAddr{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyUnixAddr) MarshalJSON(dest *json.Stream) {
	orig.UnixAddr.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyUnixAddr) UnmarshalJSON(iter *json.Iterator) {
	orig.UnixAddr.UnmarshalJSON(iter)
}

func (orig *LazyUnixAddr) SizeProto() int {
	return orig.UnixAddr.SizeProto()
}

func (orig *LazyUnixAddr) MarshalProto(buf []byte) int {
	return orig.UnixAddr.MarshalProto(buf)
}

func (orig *LazyUnixAddr) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyUnixAddr) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.UnixAddr.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.UnixAddr.Reset()
	return nil
}

func (orig *LazyUnixAddr) FinishUnmarshal(buf *UnixAddr) (*UnixAddr, error) {
	if len(orig.bytes) == 0 {
		return &orig.UnixAddr, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyUnixAddr() *LazyUnixAddr {
	orig := NewLazyUnixAddr()
	orig.UnixAddr = *GenTestUnixAddr()
	return orig
}

func GenTestLazyUnixAddrPtrSlice() []*LazyUnixAddr {
	orig := make([]*LazyUnixAddr, 5)
	orig[0] = NewLazyUnixAddr()
	orig[1] = GenTestLazyUnixAddr()
	orig[2] = NewLazyUnixAddr()
	orig[3] = GenTestLazyUnixAddr()
	orig[4] = NewLazyUnixAddr()
	return orig
}

func GenTestLazyUnixAddrSlice() []LazyUnixAddr {
	orig := make([]LazyUnixAddr, 5)
	orig[1] = *GenTestLazyUnixAddr()
	orig[3] = *GenTestLazyUnixAddr()
	return orig
}
