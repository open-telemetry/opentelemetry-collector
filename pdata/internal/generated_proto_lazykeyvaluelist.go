// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// KeyValueList is a list of KeyValue messages. We need KeyValueList as a message since oneof in AnyValue does not allow repeated fields.
type LazyKeyValueList struct {
	KeyValueList
	bytes []byte
}

var (
	protoPoolLazyKeyValueList = sync.Pool{
		New: func() any {
			return &LazyKeyValueList{}
		},
	}
)

func NewLazyKeyValueList() *LazyKeyValueList {
	if !UseProtoPooling.IsEnabled() {
		return &LazyKeyValueList{}
	}
	return protoPoolLazyKeyValueList.Get().(*LazyKeyValueList)
}

func DeleteLazyKeyValueList(orig *LazyKeyValueList, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteKeyValueList(&orig.KeyValueList, false)

	orig.Reset()
	if nullable {
		protoPoolLazyKeyValueList.Put(orig)
	}
}

func CopyLazyKeyValueList(dest, src *LazyKeyValueList) *LazyKeyValueList {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyKeyValueList()
	}

	CopyKeyValueList(&dest.KeyValueList, &src.KeyValueList)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyKeyValueListSlice(dest, src []LazyKeyValueList) []LazyKeyValueList {
	var newDest []LazyKeyValueList
	if cap(dest) < len(src) {
		newDest = make([]LazyKeyValueList, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyKeyValueList(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyKeyValueList(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyKeyValueListPtrSlice(dest, src []*LazyKeyValueList) []*LazyKeyValueList {
	var newDest []*LazyKeyValueList
	if cap(dest) < len(src) {
		newDest = make([]*LazyKeyValueList, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyKeyValueList()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyKeyValueList(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyKeyValueList()
		}
	}
	for i := range src {
		CopyLazyKeyValueList(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyKeyValueList) Reset() {
	*orig = LazyKeyValueList{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyKeyValueList) MarshalJSON(dest *json.Stream) {
	orig.KeyValueList.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyKeyValueList) UnmarshalJSON(iter *json.Iterator) {
	orig.KeyValueList.UnmarshalJSON(iter)
}

func (orig *LazyKeyValueList) SizeProto() int {
	return orig.KeyValueList.SizeProto()
}

func (orig *LazyKeyValueList) MarshalProto(buf []byte) int {
	return orig.KeyValueList.MarshalProto(buf)
}

func (orig *LazyKeyValueList) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyKeyValueList) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.KeyValueList.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.KeyValueList.Reset()
	return nil
}

func (orig *LazyKeyValueList) FinishUnmarshal(buf *KeyValueList) (*KeyValueList, error) {
	if len(orig.bytes) == 0 {
		return &orig.KeyValueList, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyKeyValueList() *LazyKeyValueList {
	orig := NewLazyKeyValueList()
	orig.KeyValueList = *GenTestKeyValueList()
	return orig
}

func GenTestLazyKeyValueListPtrSlice() []*LazyKeyValueList {
	orig := make([]*LazyKeyValueList, 5)
	orig[0] = NewLazyKeyValueList()
	orig[1] = GenTestLazyKeyValueList()
	orig[2] = NewLazyKeyValueList()
	orig[3] = GenTestLazyKeyValueList()
	orig[4] = NewLazyKeyValueList()
	return orig
}

func GenTestLazyKeyValueListSlice() []LazyKeyValueList {
	orig := make([]LazyKeyValueList, 5)
	orig[1] = *GenTestLazyKeyValueList()
	orig[3] = *GenTestLazyKeyValueList()
	return orig
}
