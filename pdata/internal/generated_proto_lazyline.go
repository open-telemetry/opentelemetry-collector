// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// Line details a specific line in a source code, linked to a function.
type LazyLine struct {
	bytes []byte
	Line
}

var (
	protoPoolLazyLine = sync.Pool{
		New: func() any {
			return &LazyLine{}
		},
	}
)

func NewLazyLine() *LazyLine {
	if !UseProtoPooling.IsEnabled() {
		return &LazyLine{}
	}
	return protoPoolLazyLine.Get().(*LazyLine)
}

func DeleteLazyLine(orig *LazyLine, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteLine(&orig.Line, false)

	orig.Reset()
	if nullable {
		protoPoolLazyLine.Put(orig)
	}
}

func CopyLazyLine(dest, src *LazyLine) *LazyLine {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyLine()
	}

	CopyLine(&dest.Line, &src.Line)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyLineSlice(dest, src []LazyLine) []LazyLine {
	var newDest []LazyLine
	if cap(dest) < len(src) {
		newDest = make([]LazyLine, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyLine(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyLine(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyLinePtrSlice(dest, src []*LazyLine) []*LazyLine {
	var newDest []*LazyLine
	if cap(dest) < len(src) {
		newDest = make([]*LazyLine, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyLine()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyLine(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyLine()
		}
	}
	for i := range src {
		CopyLazyLine(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyLine) Reset() {
	*orig = LazyLine{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyLine) MarshalJSON(dest *json.Stream) {
	orig.Line.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyLine) UnmarshalJSON(iter *json.Iterator) {
	orig.Line.UnmarshalJSON(iter)
}

func (orig *LazyLine) SizeProto() int {
	return orig.Line.SizeProto()
}

func (orig *LazyLine) MarshalProto(buf []byte) int {
	return orig.Line.MarshalProto(buf)
}

func (orig *LazyLine) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyLine) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.Line.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.Line.Reset()
	return nil
}

func (orig *LazyLine) FinishUnmarshal(buf *Line) (*Line, error) {
	if len(orig.bytes) == 0 {
		return &orig.Line, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyLine() *LazyLine {
	orig := NewLazyLine()
	orig.Line = *GenTestLine()
	return orig
}

func GenTestLazyLinePtrSlice() []*LazyLine {
	orig := make([]*LazyLine, 5)
	orig[0] = NewLazyLine()
	orig[1] = GenTestLazyLine()
	orig[2] = NewLazyLine()
	orig[3] = GenTestLazyLine()
	orig[4] = NewLazyLine()
	return orig
}

func GenTestLazyLineSlice() []LazyLine {
	orig := make([]LazyLine, 5)
	orig[1] = *GenTestLazyLine()
	orig[3] = *GenTestLazyLine()
	return orig
}
