// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// Profile are an implementation of the pprofextended data model.

type LazyProfile struct {
	Profile
	bytes []byte
}

var (
	protoPoolLazyProfile = sync.Pool{
		New: func() any {
			return &LazyProfile{}
		},
	}
)

func NewLazyProfile() *LazyProfile {
	if !UseProtoPooling.IsEnabled() {
		return &LazyProfile{}
	}
	return protoPoolLazyProfile.Get().(*LazyProfile)
}

func DeleteLazyProfile(orig *LazyProfile, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteProfile(&orig.Profile, false)

	orig.Reset()
	if nullable {
		protoPoolLazyProfile.Put(orig)
	}
}

func CopyLazyProfile(dest, src *LazyProfile) *LazyProfile {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyProfile()
	}

	CopyProfile(&dest.Profile, &src.Profile)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyProfileSlice(dest, src []LazyProfile) []LazyProfile {
	var newDest []LazyProfile
	if cap(dest) < len(src) {
		newDest = make([]LazyProfile, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyProfile(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyProfile(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyProfilePtrSlice(dest, src []*LazyProfile) []*LazyProfile {
	var newDest []*LazyProfile
	if cap(dest) < len(src) {
		newDest = make([]*LazyProfile, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyProfile()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyProfile(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyProfile()
		}
	}
	for i := range src {
		CopyLazyProfile(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyProfile) Reset() {
	*orig = LazyProfile{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyProfile) MarshalJSON(dest *json.Stream) {
	orig.Profile.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyProfile) UnmarshalJSON(iter *json.Iterator) {
	orig.Profile.UnmarshalJSON(iter)
}

func (orig *LazyProfile) SizeProto() int {
	return orig.Profile.SizeProto()
}

func (orig *LazyProfile) MarshalProto(buf []byte) int {
	return orig.Profile.MarshalProto(buf)
}

func (orig *LazyProfile) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyProfile) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.Profile.UnmarshalProtoOpts(buf, opts)
	}
	if err := SkipProfileProto(buf); err != nil {
		return err
	}
	orig.bytes = buf
	orig.Profile.Reset()
	return nil
}

func SkipLazyProfileProto(buf []byte) error {
	return SkipProfileProto(buf)
}

func (orig *LazyProfile) FinishUnmarshal(buf *Profile) (*Profile, error) {
	if len(orig.bytes) == 0 {
		return &orig.Profile, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyProfile() *LazyProfile {
	orig := NewLazyProfile()
	orig.Profile = *GenTestProfile()
	return orig
}

func GenTestLazyProfilePtrSlice() []*LazyProfile {
	orig := make([]*LazyProfile, 5)
	orig[0] = NewLazyProfile()
	orig[1] = GenTestLazyProfile()
	orig[2] = NewLazyProfile()
	orig[3] = GenTestLazyProfile()
	orig[4] = NewLazyProfile()
	return orig
}

func GenTestLazyProfileSlice() []LazyProfile {
	orig := make([]LazyProfile, 5)
	orig[1] = *GenTestLazyProfile()
	orig[3] = *GenTestLazyProfile()
	return orig
}
