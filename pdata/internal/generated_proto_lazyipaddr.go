// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

type LazyIPAddr struct {
	IPAddr
	bytes []byte
}

var (
	protoPoolLazyIPAddr = sync.Pool{
		New: func() any {
			return &LazyIPAddr{}
		},
	}
)

func NewLazyIPAddr() *LazyIPAddr {
	if !UseProtoPooling.IsEnabled() {
		return &LazyIPAddr{}
	}
	return protoPoolLazyIPAddr.Get().(*LazyIPAddr)
}

func DeleteLazyIPAddr(orig *LazyIPAddr, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteIPAddr(&orig.IPAddr, false)

	orig.Reset()
	if nullable {
		protoPoolLazyIPAddr.Put(orig)
	}
}

func CopyLazyIPAddr(dest, src *LazyIPAddr) *LazyIPAddr {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyIPAddr()
	}

	CopyIPAddr(&dest.IPAddr, &src.IPAddr)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyIPAddrSlice(dest, src []LazyIPAddr) []LazyIPAddr {
	var newDest []LazyIPAddr
	if cap(dest) < len(src) {
		newDest = make([]LazyIPAddr, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyIPAddr(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyIPAddr(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyIPAddrPtrSlice(dest, src []*LazyIPAddr) []*LazyIPAddr {
	var newDest []*LazyIPAddr
	if cap(dest) < len(src) {
		newDest = make([]*LazyIPAddr, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyIPAddr()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyIPAddr(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyIPAddr()
		}
	}
	for i := range src {
		CopyLazyIPAddr(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyIPAddr) Reset() {
	*orig = LazyIPAddr{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyIPAddr) MarshalJSON(dest *json.Stream) {
	orig.IPAddr.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyIPAddr) UnmarshalJSON(iter *json.Iterator) {
	orig.IPAddr.UnmarshalJSON(iter)
}

func (orig *LazyIPAddr) SizeProto() int {
	return orig.IPAddr.SizeProto()
}

func (orig *LazyIPAddr) MarshalProto(buf []byte) int {
	return orig.IPAddr.MarshalProto(buf)
}

func (orig *LazyIPAddr) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyIPAddr) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.IPAddr.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.IPAddr.Reset()
	return nil
}

func (orig *LazyIPAddr) FinishUnmarshal(buf *IPAddr) (*IPAddr, error) {
	if len(orig.bytes) == 0 {
		return &orig.IPAddr, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyIPAddr() *LazyIPAddr {
	orig := NewLazyIPAddr()
	orig.IPAddr = *GenTestIPAddr()
	return orig
}

func GenTestLazyIPAddrPtrSlice() []*LazyIPAddr {
	orig := make([]*LazyIPAddr, 5)
	orig[0] = NewLazyIPAddr()
	orig[1] = GenTestLazyIPAddr()
	orig[2] = NewLazyIPAddr()
	orig[3] = GenTestLazyIPAddr()
	orig[4] = NewLazyIPAddr()
	return orig
}

func GenTestLazyIPAddrSlice() []LazyIPAddr {
	orig := make([]LazyIPAddr, 5)
	orig[1] = *GenTestLazyIPAddr()
	orig[3] = *GenTestLazyIPAddr()
	return orig
}
