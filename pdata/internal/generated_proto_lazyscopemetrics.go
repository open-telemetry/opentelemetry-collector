// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// ScopeMetrics is a collection of metrics from a LibraryInstrumentation.
type LazyScopeMetrics struct {
	ScopeMetrics
	bytes []byte
}

var (
	protoPoolLazyScopeMetrics = sync.Pool{
		New: func() any {
			return &LazyScopeMetrics{}
		},
	}
)

func NewLazyScopeMetrics() *LazyScopeMetrics {
	if !UseProtoPooling.IsEnabled() {
		return &LazyScopeMetrics{}
	}
	return protoPoolLazyScopeMetrics.Get().(*LazyScopeMetrics)
}

func DeleteLazyScopeMetrics(orig *LazyScopeMetrics, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteScopeMetrics(&orig.ScopeMetrics, false)

	orig.Reset()
	if nullable {
		protoPoolLazyScopeMetrics.Put(orig)
	}
}

func CopyLazyScopeMetrics(dest, src *LazyScopeMetrics) *LazyScopeMetrics {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyScopeMetrics()
	}

	CopyScopeMetrics(&dest.ScopeMetrics, &src.ScopeMetrics)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyScopeMetricsSlice(dest, src []LazyScopeMetrics) []LazyScopeMetrics {
	var newDest []LazyScopeMetrics
	if cap(dest) < len(src) {
		newDest = make([]LazyScopeMetrics, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyScopeMetrics(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyScopeMetrics(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyScopeMetricsPtrSlice(dest, src []*LazyScopeMetrics) []*LazyScopeMetrics {
	var newDest []*LazyScopeMetrics
	if cap(dest) < len(src) {
		newDest = make([]*LazyScopeMetrics, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyScopeMetrics()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyScopeMetrics(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyScopeMetrics()
		}
	}
	for i := range src {
		CopyLazyScopeMetrics(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyScopeMetrics) Reset() {
	*orig = LazyScopeMetrics{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyScopeMetrics) MarshalJSON(dest *json.Stream) {
	orig.ScopeMetrics.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyScopeMetrics) UnmarshalJSON(iter *json.Iterator) {
	orig.ScopeMetrics.UnmarshalJSON(iter)
}

func (orig *LazyScopeMetrics) SizeProto() int {
	return orig.ScopeMetrics.SizeProto()
}

func (orig *LazyScopeMetrics) MarshalProto(buf []byte) int {
	return orig.ScopeMetrics.MarshalProto(buf)
}

func (orig *LazyScopeMetrics) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyScopeMetrics) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.ScopeMetrics.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.ScopeMetrics.Reset()
	return nil
}

func (orig *LazyScopeMetrics) FinishUnmarshal(buf *ScopeMetrics) (*ScopeMetrics, error) {
	if len(orig.bytes) == 0 {
		return &orig.ScopeMetrics, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyScopeMetrics() *LazyScopeMetrics {
	orig := NewLazyScopeMetrics()
	orig.ScopeMetrics = *GenTestScopeMetrics()
	return orig
}

func GenTestLazyScopeMetricsPtrSlice() []*LazyScopeMetrics {
	orig := make([]*LazyScopeMetrics, 5)
	orig[0] = NewLazyScopeMetrics()
	orig[1] = GenTestLazyScopeMetrics()
	orig[2] = NewLazyScopeMetrics()
	orig[3] = GenTestLazyScopeMetrics()
	orig[4] = NewLazyScopeMetrics()
	return orig
}

func GenTestLazyScopeMetricsSlice() []LazyScopeMetrics {
	orig := make([]LazyScopeMetrics, 5)
	orig[1] = *GenTestLazyScopeMetrics()
	orig[3] = *GenTestLazyScopeMetrics()
	return orig
}
