// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

type LazyEntityRef struct {
	EntityRef
	bytes []byte
}

var (
	protoPoolLazyEntityRef = sync.Pool{
		New: func() any {
			return &LazyEntityRef{}
		},
	}
)

func NewLazyEntityRef() *LazyEntityRef {
	if !UseProtoPooling.IsEnabled() {
		return &LazyEntityRef{}
	}
	return protoPoolLazyEntityRef.Get().(*LazyEntityRef)
}

func DeleteLazyEntityRef(orig *LazyEntityRef, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteEntityRef(&orig.EntityRef, false)

	orig.Reset()
	if nullable {
		protoPoolLazyEntityRef.Put(orig)
	}
}

func CopyLazyEntityRef(dest, src *LazyEntityRef) *LazyEntityRef {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyEntityRef()
	}

	CopyEntityRef(&dest.EntityRef, &src.EntityRef)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyEntityRefSlice(dest, src []LazyEntityRef) []LazyEntityRef {
	var newDest []LazyEntityRef
	if cap(dest) < len(src) {
		newDest = make([]LazyEntityRef, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyEntityRef(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyEntityRef(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyEntityRefPtrSlice(dest, src []*LazyEntityRef) []*LazyEntityRef {
	var newDest []*LazyEntityRef
	if cap(dest) < len(src) {
		newDest = make([]*LazyEntityRef, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyEntityRef()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyEntityRef(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyEntityRef()
		}
	}
	for i := range src {
		CopyLazyEntityRef(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyEntityRef) Reset() {
	*orig = LazyEntityRef{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyEntityRef) MarshalJSON(dest *json.Stream) {
	orig.EntityRef.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyEntityRef) UnmarshalJSON(iter *json.Iterator) {
	orig.EntityRef.UnmarshalJSON(iter)
}

func (orig *LazyEntityRef) SizeProto() int {
	return orig.EntityRef.SizeProto()
}

func (orig *LazyEntityRef) MarshalProto(buf []byte) int {
	return orig.EntityRef.MarshalProto(buf)
}

func (orig *LazyEntityRef) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyEntityRef) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.EntityRef.UnmarshalProtoOpts(buf, opts)
	}
	if err := SkipEntityRefProto(buf); err != nil {
		return err
	}
	orig.bytes = buf
	orig.EntityRef.Reset()
	return nil
}

func SkipLazyEntityRefProto(buf []byte) error {
	return SkipEntityRefProto(buf)
}

func (orig *LazyEntityRef) FinishUnmarshal(buf *EntityRef) (*EntityRef, error) {
	if len(orig.bytes) == 0 {
		return &orig.EntityRef, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyEntityRef() *LazyEntityRef {
	orig := NewLazyEntityRef()
	orig.EntityRef = *GenTestEntityRef()
	return orig
}

func GenTestLazyEntityRefPtrSlice() []*LazyEntityRef {
	orig := make([]*LazyEntityRef, 5)
	orig[0] = NewLazyEntityRef()
	orig[1] = GenTestLazyEntityRef()
	orig[2] = NewLazyEntityRef()
	orig[3] = GenTestLazyEntityRef()
	orig[4] = NewLazyEntityRef()
	return orig
}

func GenTestLazyEntityRefSlice() []LazyEntityRef {
	orig := make([]LazyEntityRef, 5)
	orig[1] = *GenTestLazyEntityRef()
	orig[3] = *GenTestLazyEntityRef()
	return orig
}
