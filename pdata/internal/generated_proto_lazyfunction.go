// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// Function describes a function, including its human-readable name, system name, source file, and starting line number in the source.
type LazyFunction struct {
	bytes []byte
	Function
}

var (
	protoPoolLazyFunction = sync.Pool{
		New: func() any {
			return &LazyFunction{}
		},
	}
)

func NewLazyFunction() *LazyFunction {
	if !UseProtoPooling.IsEnabled() {
		return &LazyFunction{}
	}
	return protoPoolLazyFunction.Get().(*LazyFunction)
}

func DeleteLazyFunction(orig *LazyFunction, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteFunction(&orig.Function, false)

	orig.Reset()
	if nullable {
		protoPoolLazyFunction.Put(orig)
	}
}

func CopyLazyFunction(dest, src *LazyFunction) *LazyFunction {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyFunction()
	}

	CopyFunction(&dest.Function, &src.Function)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyFunctionSlice(dest, src []LazyFunction) []LazyFunction {
	var newDest []LazyFunction
	if cap(dest) < len(src) {
		newDest = make([]LazyFunction, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyFunction(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyFunction(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyFunctionPtrSlice(dest, src []*LazyFunction) []*LazyFunction {
	var newDest []*LazyFunction
	if cap(dest) < len(src) {
		newDest = make([]*LazyFunction, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyFunction()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyFunction(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyFunction()
		}
	}
	for i := range src {
		CopyLazyFunction(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyFunction) Reset() {
	*orig = LazyFunction{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyFunction) MarshalJSON(dest *json.Stream) {
	orig.Function.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyFunction) UnmarshalJSON(iter *json.Iterator) {
	orig.Function.UnmarshalJSON(iter)
}

func (orig *LazyFunction) SizeProto() int {
	return orig.Function.SizeProto()
}

func (orig *LazyFunction) MarshalProto(buf []byte) int {
	return orig.Function.MarshalProto(buf)
}

func (orig *LazyFunction) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyFunction) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.Function.UnmarshalProtoOpts(buf, opts)
	}
	if err := SkipFunctionProto(buf); err != nil {
		return err
	}
	orig.bytes = buf
	orig.Function.Reset()
	return nil
}

func SkipLazyFunctionProto(buf []byte) error {
	return SkipFunctionProto(buf)
}

func (orig *LazyFunction) FinishUnmarshal(buf *Function) (*Function, error) {
	if len(orig.bytes) == 0 {
		return &orig.Function, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyFunction() *LazyFunction {
	orig := NewLazyFunction()
	orig.Function = *GenTestFunction()
	return orig
}

func GenTestLazyFunctionPtrSlice() []*LazyFunction {
	orig := make([]*LazyFunction, 5)
	orig[0] = NewLazyFunction()
	orig[1] = GenTestLazyFunction()
	orig[2] = NewLazyFunction()
	orig[3] = GenTestLazyFunction()
	orig[4] = NewLazyFunction()
	return orig
}

func GenTestLazyFunctionSlice() []LazyFunction {
	orig := make([]LazyFunction, 5)
	orig[1] = *GenTestLazyFunction()
	orig[3] = *GenTestLazyFunction()
	return orig
}
