// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// Span represents a single operation within a trace.
// See Span definition in OTLP: https://github.com/open-telemetry/opentelemetry-proto/blob/main/opentelemetry/proto/trace/v1/trace.proto
type LazySpan struct {
	Span
	bytes []byte
}

var (
	protoPoolLazySpan = sync.Pool{
		New: func() any {
			return &LazySpan{}
		},
	}
)

func NewLazySpan() *LazySpan {
	if !UseProtoPooling.IsEnabled() {
		return &LazySpan{}
	}
	return protoPoolLazySpan.Get().(*LazySpan)
}

func DeleteLazySpan(orig *LazySpan, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteSpan(&orig.Span, false)

	orig.Reset()
	if nullable {
		protoPoolLazySpan.Put(orig)
	}
}

func CopyLazySpan(dest, src *LazySpan) *LazySpan {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazySpan()
	}

	CopySpan(&dest.Span, &src.Span)
	dest.bytes = src.bytes

	return dest
}

func CopyLazySpanSlice(dest, src []LazySpan) []LazySpan {
	var newDest []LazySpan
	if cap(dest) < len(src) {
		newDest = make([]LazySpan, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazySpan(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazySpan(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazySpanPtrSlice(dest, src []*LazySpan) []*LazySpan {
	var newDest []*LazySpan
	if cap(dest) < len(src) {
		newDest = make([]*LazySpan, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazySpan()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazySpan(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazySpan()
		}
	}
	for i := range src {
		CopyLazySpan(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazySpan) Reset() {
	*orig = LazySpan{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazySpan) MarshalJSON(dest *json.Stream) {
	orig.Span.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazySpan) UnmarshalJSON(iter *json.Iterator) {
	orig.Span.UnmarshalJSON(iter)
}

func (orig *LazySpan) SizeProto() int {
	return orig.Span.SizeProto()
}

func (orig *LazySpan) MarshalProto(buf []byte) int {
	return orig.Span.MarshalProto(buf)
}

func (orig *LazySpan) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazySpan) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.Span.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.Span.Reset()
	return nil
}

func (orig *LazySpan) FinishUnmarshal(buf *Span) (*Span, error) {
	if len(orig.bytes) == 0 {
		return &orig.Span, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazySpan() *LazySpan {
	orig := NewLazySpan()
	orig.Span = *GenTestSpan()
	return orig
}

func GenTestLazySpanPtrSlice() []*LazySpan {
	orig := make([]*LazySpan, 5)
	orig[0] = NewLazySpan()
	orig[1] = GenTestLazySpan()
	orig[2] = NewLazySpan()
	orig[3] = GenTestLazySpan()
	orig[4] = NewLazySpan()
	return orig
}

func GenTestLazySpanSlice() []LazySpan {
	orig := make([]LazySpan, 5)
	orig[1] = *GenTestLazySpan()
	orig[3] = *GenTestLazySpan()
	return orig
}
