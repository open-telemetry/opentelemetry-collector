// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// Resource is a message representing the resource information.
type LazyResource struct {
	Resource
	bytes []byte
}

var (
	protoPoolLazyResource = sync.Pool{
		New: func() any {
			return &LazyResource{}
		},
	}
)

func NewLazyResource() *LazyResource {
	if !UseProtoPooling.IsEnabled() {
		return &LazyResource{}
	}
	return protoPoolLazyResource.Get().(*LazyResource)
}

func DeleteLazyResource(orig *LazyResource, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteResource(&orig.Resource, false)

	orig.Reset()
	if nullable {
		protoPoolLazyResource.Put(orig)
	}
}

func CopyLazyResource(dest, src *LazyResource) *LazyResource {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyResource()
	}

	CopyResource(&dest.Resource, &src.Resource)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyResourceSlice(dest, src []LazyResource) []LazyResource {
	var newDest []LazyResource
	if cap(dest) < len(src) {
		newDest = make([]LazyResource, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyResource(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyResource(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyResourcePtrSlice(dest, src []*LazyResource) []*LazyResource {
	var newDest []*LazyResource
	if cap(dest) < len(src) {
		newDest = make([]*LazyResource, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyResource()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyResource(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyResource()
		}
	}
	for i := range src {
		CopyLazyResource(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyResource) Reset() {
	*orig = LazyResource{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyResource) MarshalJSON(dest *json.Stream) {
	orig.Resource.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyResource) UnmarshalJSON(iter *json.Iterator) {
	orig.Resource.UnmarshalJSON(iter)
}

func (orig *LazyResource) SizeProto() int {
	return orig.Resource.SizeProto()
}

func (orig *LazyResource) MarshalProto(buf []byte) int {
	return orig.Resource.MarshalProto(buf)
}

func (orig *LazyResource) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyResource) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.Resource.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.Resource.Reset()
	return nil
}

func (orig *LazyResource) FinishUnmarshal(buf *Resource) (*Resource, error) {
	if len(orig.bytes) == 0 {
		return &orig.Resource, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyResource() *LazyResource {
	orig := NewLazyResource()
	orig.Resource = *GenTestResource()
	return orig
}

func GenTestLazyResourcePtrSlice() []*LazyResource {
	orig := make([]*LazyResource, 5)
	orig[0] = NewLazyResource()
	orig[1] = GenTestLazyResource()
	orig[2] = NewLazyResource()
	orig[3] = GenTestLazyResource()
	orig[4] = NewLazyResource()
	return orig
}

func GenTestLazyResourceSlice() []LazyResource {
	orig := make([]LazyResource, 5)
	orig[1] = *GenTestLazyResource()
	orig[3] = *GenTestLazyResource()
	return orig
}
