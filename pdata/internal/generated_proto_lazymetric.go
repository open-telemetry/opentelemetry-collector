// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// Metric represents one metric as a collection of datapoints.
// See Metric definition in OTLP: https://github.com/open-telemetry/opentelemetry-proto/blob/main/opentelemetry/proto/metrics/v1/metrics.proto
type LazyMetric struct {
	Metric
	bytes []byte
}

var (
	protoPoolLazyMetric = sync.Pool{
		New: func() any {
			return &LazyMetric{}
		},
	}
)

func NewLazyMetric() *LazyMetric {
	if !UseProtoPooling.IsEnabled() {
		return &LazyMetric{}
	}
	return protoPoolLazyMetric.Get().(*LazyMetric)
}

func DeleteLazyMetric(orig *LazyMetric, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteMetric(&orig.Metric, false)

	orig.Reset()
	if nullable {
		protoPoolLazyMetric.Put(orig)
	}
}

func CopyLazyMetric(dest, src *LazyMetric) *LazyMetric {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyMetric()
	}

	CopyMetric(&dest.Metric, &src.Metric)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyMetricSlice(dest, src []LazyMetric) []LazyMetric {
	var newDest []LazyMetric
	if cap(dest) < len(src) {
		newDest = make([]LazyMetric, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyMetric(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyMetric(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyMetricPtrSlice(dest, src []*LazyMetric) []*LazyMetric {
	var newDest []*LazyMetric
	if cap(dest) < len(src) {
		newDest = make([]*LazyMetric, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyMetric()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyMetric(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyMetric()
		}
	}
	for i := range src {
		CopyLazyMetric(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyMetric) Reset() {
	*orig = LazyMetric{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyMetric) MarshalJSON(dest *json.Stream) {
	orig.Metric.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyMetric) UnmarshalJSON(iter *json.Iterator) {
	orig.Metric.UnmarshalJSON(iter)
}

func (orig *LazyMetric) SizeProto() int {
	return orig.Metric.SizeProto()
}

func (orig *LazyMetric) MarshalProto(buf []byte) int {
	return orig.Metric.MarshalProto(buf)
}

func (orig *LazyMetric) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyMetric) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.Metric.UnmarshalProtoOpts(buf, opts)
	}
	if err := SkipMetricProto(buf); err != nil {
		return err
	}
	orig.bytes = buf
	orig.Metric.Reset()
	return nil
}

func SkipLazyMetricProto(buf []byte) error {
	return SkipMetricProto(buf)
}

func (orig *LazyMetric) FinishUnmarshal(buf *Metric) (*Metric, error) {
	if len(orig.bytes) == 0 {
		return &orig.Metric, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyMetric() *LazyMetric {
	orig := NewLazyMetric()
	orig.Metric = *GenTestMetric()
	return orig
}

func GenTestLazyMetricPtrSlice() []*LazyMetric {
	orig := make([]*LazyMetric, 5)
	orig[0] = NewLazyMetric()
	orig[1] = GenTestLazyMetric()
	orig[2] = NewLazyMetric()
	orig[3] = GenTestLazyMetric()
	orig[4] = NewLazyMetric()
	return orig
}

func GenTestLazyMetricSlice() []LazyMetric {
	orig := make([]LazyMetric, 5)
	orig[1] = *GenTestLazyMetric()
	orig[3] = *GenTestLazyMetric()
	return orig
}
