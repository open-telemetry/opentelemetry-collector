// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// Histogram represents the type of a metric that is calculated by aggregating as a Histogram of all reported measurements over a time interval.
type LazyHistogram struct {
	bytes []byte
	Histogram
}

var (
	protoPoolLazyHistogram = sync.Pool{
		New: func() any {
			return &LazyHistogram{}
		},
	}
)

func NewLazyHistogram() *LazyHistogram {
	if !UseProtoPooling.IsEnabled() {
		return &LazyHistogram{}
	}
	return protoPoolLazyHistogram.Get().(*LazyHistogram)
}

func DeleteLazyHistogram(orig *LazyHistogram, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteHistogram(&orig.Histogram, false)

	orig.Reset()
	if nullable {
		protoPoolLazyHistogram.Put(orig)
	}
}

func CopyLazyHistogram(dest, src *LazyHistogram) *LazyHistogram {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyHistogram()
	}

	CopyHistogram(&dest.Histogram, &src.Histogram)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyHistogramSlice(dest, src []LazyHistogram) []LazyHistogram {
	var newDest []LazyHistogram
	if cap(dest) < len(src) {
		newDest = make([]LazyHistogram, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyHistogram(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyHistogram(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyHistogramPtrSlice(dest, src []*LazyHistogram) []*LazyHistogram {
	var newDest []*LazyHistogram
	if cap(dest) < len(src) {
		newDest = make([]*LazyHistogram, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyHistogram()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyHistogram(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyHistogram()
		}
	}
	for i := range src {
		CopyLazyHistogram(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyHistogram) Reset() {
	*orig = LazyHistogram{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyHistogram) MarshalJSON(dest *json.Stream) {
	orig.Histogram.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyHistogram) UnmarshalJSON(iter *json.Iterator) {
	orig.Histogram.UnmarshalJSON(iter)
}

func (orig *LazyHistogram) SizeProto() int {
	return orig.Histogram.SizeProto()
}

func (orig *LazyHistogram) MarshalProto(buf []byte) int {
	return orig.Histogram.MarshalProto(buf)
}

func (orig *LazyHistogram) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyHistogram) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.Histogram.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.Histogram.Reset()
	return nil
}

func (orig *LazyHistogram) FinishUnmarshal(buf *Histogram) (*Histogram, error) {
	if len(orig.bytes) == 0 {
		return &orig.Histogram, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyHistogram() *LazyHistogram {
	orig := NewLazyHistogram()
	orig.Histogram = *GenTestHistogram()
	return orig
}

func GenTestLazyHistogramPtrSlice() []*LazyHistogram {
	orig := make([]*LazyHistogram, 5)
	orig[0] = NewLazyHistogram()
	orig[1] = GenTestLazyHistogram()
	orig[2] = NewLazyHistogram()
	orig[3] = GenTestLazyHistogram()
	orig[4] = NewLazyHistogram()
	return orig
}

func GenTestLazyHistogramSlice() []LazyHistogram {
	orig := make([]LazyHistogram, 5)
	orig[1] = *GenTestLazyHistogram()
	orig[3] = *GenTestLazyHistogram()
	return orig
}
