// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

type LazyKeyValue struct {
	KeyValue
	bytes []byte
}

var (
	protoPoolLazyKeyValue = sync.Pool{
		New: func() any {
			return &LazyKeyValue{}
		},
	}
)

func NewLazyKeyValue() *LazyKeyValue {
	if !UseProtoPooling.IsEnabled() {
		return &LazyKeyValue{}
	}
	return protoPoolLazyKeyValue.Get().(*LazyKeyValue)
}

func DeleteLazyKeyValue(orig *LazyKeyValue, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteKeyValue(&orig.KeyValue, false)

	orig.Reset()
	if nullable {
		protoPoolLazyKeyValue.Put(orig)
	}
}

func CopyLazyKeyValue(dest, src *LazyKeyValue) *LazyKeyValue {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyKeyValue()
	}

	CopyKeyValue(&dest.KeyValue, &src.KeyValue)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyKeyValueSlice(dest, src []LazyKeyValue) []LazyKeyValue {
	var newDest []LazyKeyValue
	if cap(dest) < len(src) {
		newDest = make([]LazyKeyValue, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyKeyValue(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyKeyValue(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyKeyValuePtrSlice(dest, src []*LazyKeyValue) []*LazyKeyValue {
	var newDest []*LazyKeyValue
	if cap(dest) < len(src) {
		newDest = make([]*LazyKeyValue, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyKeyValue()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyKeyValue(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyKeyValue()
		}
	}
	for i := range src {
		CopyLazyKeyValue(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyKeyValue) Reset() {
	*orig = LazyKeyValue{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyKeyValue) MarshalJSON(dest *json.Stream) {
	orig.KeyValue.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyKeyValue) UnmarshalJSON(iter *json.Iterator) {
	orig.KeyValue.UnmarshalJSON(iter)
}

func (orig *LazyKeyValue) SizeProto() int {
	return orig.KeyValue.SizeProto()
}

func (orig *LazyKeyValue) MarshalProto(buf []byte) int {
	return orig.KeyValue.MarshalProto(buf)
}

func (orig *LazyKeyValue) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyKeyValue) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.KeyValue.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.KeyValue.Reset()
	return nil
}

func (orig *LazyKeyValue) FinishUnmarshal(buf *KeyValue) (*KeyValue, error) {
	if len(orig.bytes) == 0 {
		return &orig.KeyValue, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyKeyValue() *LazyKeyValue {
	orig := NewLazyKeyValue()
	orig.KeyValue = *GenTestKeyValue()
	return orig
}

func GenTestLazyKeyValuePtrSlice() []*LazyKeyValue {
	orig := make([]*LazyKeyValue, 5)
	orig[0] = NewLazyKeyValue()
	orig[1] = GenTestLazyKeyValue()
	orig[2] = NewLazyKeyValue()
	orig[3] = GenTestLazyKeyValue()
	orig[4] = NewLazyKeyValue()
	return orig
}

func GenTestLazyKeyValueSlice() []LazyKeyValue {
	orig := make([]LazyKeyValue, 5)
	orig[1] = *GenTestLazyKeyValue()
	orig[3] = *GenTestLazyKeyValue()
	return orig
}
