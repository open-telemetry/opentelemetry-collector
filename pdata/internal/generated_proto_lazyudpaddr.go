// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

type LazyUDPAddr struct {
	UDPAddr
	bytes []byte
}

var (
	protoPoolLazyUDPAddr = sync.Pool{
		New: func() any {
			return &LazyUDPAddr{}
		},
	}
)

func NewLazyUDPAddr() *LazyUDPAddr {
	if !UseProtoPooling.IsEnabled() {
		return &LazyUDPAddr{}
	}
	return protoPoolLazyUDPAddr.Get().(*LazyUDPAddr)
}

func DeleteLazyUDPAddr(orig *LazyUDPAddr, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteUDPAddr(&orig.UDPAddr, false)

	orig.Reset()
	if nullable {
		protoPoolLazyUDPAddr.Put(orig)
	}
}

func CopyLazyUDPAddr(dest, src *LazyUDPAddr) *LazyUDPAddr {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyUDPAddr()
	}

	CopyUDPAddr(&dest.UDPAddr, &src.UDPAddr)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyUDPAddrSlice(dest, src []LazyUDPAddr) []LazyUDPAddr {
	var newDest []LazyUDPAddr
	if cap(dest) < len(src) {
		newDest = make([]LazyUDPAddr, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyUDPAddr(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyUDPAddr(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyUDPAddrPtrSlice(dest, src []*LazyUDPAddr) []*LazyUDPAddr {
	var newDest []*LazyUDPAddr
	if cap(dest) < len(src) {
		newDest = make([]*LazyUDPAddr, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyUDPAddr()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyUDPAddr(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyUDPAddr()
		}
	}
	for i := range src {
		CopyLazyUDPAddr(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyUDPAddr) Reset() {
	*orig = LazyUDPAddr{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyUDPAddr) MarshalJSON(dest *json.Stream) {
	orig.UDPAddr.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyUDPAddr) UnmarshalJSON(iter *json.Iterator) {
	orig.UDPAddr.UnmarshalJSON(iter)
}

func (orig *LazyUDPAddr) SizeProto() int {
	return orig.UDPAddr.SizeProto()
}

func (orig *LazyUDPAddr) MarshalProto(buf []byte) int {
	return orig.UDPAddr.MarshalProto(buf)
}

func (orig *LazyUDPAddr) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyUDPAddr) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.UDPAddr.UnmarshalProtoOpts(buf, opts)
	}
	if err := SkipUDPAddrProto(buf); err != nil {
		return err
	}
	orig.bytes = buf
	orig.UDPAddr.Reset()
	return nil
}

func SkipLazyUDPAddrProto(buf []byte) error {
	return SkipUDPAddrProto(buf)
}

func (orig *LazyUDPAddr) FinishUnmarshal(buf *UDPAddr) (*UDPAddr, error) {
	if len(orig.bytes) == 0 {
		return &orig.UDPAddr, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyUDPAddr() *LazyUDPAddr {
	orig := NewLazyUDPAddr()
	orig.UDPAddr = *GenTestUDPAddr()
	return orig
}

func GenTestLazyUDPAddrPtrSlice() []*LazyUDPAddr {
	orig := make([]*LazyUDPAddr, 5)
	orig[0] = NewLazyUDPAddr()
	orig[1] = GenTestLazyUDPAddr()
	orig[2] = NewLazyUDPAddr()
	orig[3] = GenTestLazyUDPAddr()
	orig[4] = NewLazyUDPAddr()
	return orig
}

func GenTestLazyUDPAddrSlice() []LazyUDPAddr {
	orig := make([]LazyUDPAddr, 5)
	orig[1] = *GenTestLazyUDPAddr()
	orig[3] = *GenTestLazyUDPAddr()
	return orig
}
