// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// Link represents a pointer from a profile Sample to a trace Span.
type LazyLink struct {
	bytes []byte
	Link
}

var (
	protoPoolLazyLink = sync.Pool{
		New: func() any {
			return &LazyLink{}
		},
	}
)

func NewLazyLink() *LazyLink {
	if !UseProtoPooling.IsEnabled() {
		return &LazyLink{}
	}
	return protoPoolLazyLink.Get().(*LazyLink)
}

func DeleteLazyLink(orig *LazyLink, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteLink(&orig.Link, false)

	orig.Reset()
	if nullable {
		protoPoolLazyLink.Put(orig)
	}
}

func CopyLazyLink(dest, src *LazyLink) *LazyLink {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazyLink()
	}

	CopyLink(&dest.Link, &src.Link)
	dest.bytes = src.bytes

	return dest
}

func CopyLazyLinkSlice(dest, src []LazyLink) []LazyLink {
	var newDest []LazyLink
	if cap(dest) < len(src) {
		newDest = make([]LazyLink, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyLink(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazyLink(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazyLinkPtrSlice(dest, src []*LazyLink) []*LazyLink {
	var newDest []*LazyLink
	if cap(dest) < len(src) {
		newDest = make([]*LazyLink, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyLink()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazyLink(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazyLink()
		}
	}
	for i := range src {
		CopyLazyLink(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazyLink) Reset() {
	*orig = LazyLink{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazyLink) MarshalJSON(dest *json.Stream) {
	orig.Link.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazyLink) UnmarshalJSON(iter *json.Iterator) {
	orig.Link.UnmarshalJSON(iter)
}

func (orig *LazyLink) SizeProto() int {
	return orig.Link.SizeProto()
}

func (orig *LazyLink) MarshalProto(buf []byte) int {
	return orig.Link.MarshalProto(buf)
}

func (orig *LazyLink) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazyLink) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.Link.UnmarshalProtoOpts(buf, opts)
	}
	orig.bytes = buf
	orig.Link.Reset()
	return nil
}

func (orig *LazyLink) FinishUnmarshal(buf *Link) (*Link, error) {
	if len(orig.bytes) == 0 {
		return &orig.Link, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazyLink() *LazyLink {
	orig := NewLazyLink()
	orig.Link = *GenTestLink()
	return orig
}

func GenTestLazyLinkPtrSlice() []*LazyLink {
	orig := make([]*LazyLink, 5)
	orig[0] = NewLazyLink()
	orig[1] = GenTestLazyLink()
	orig[2] = NewLazyLink()
	orig[3] = GenTestLazyLink()
	orig[4] = NewLazyLink()
	return orig
}

func GenTestLazyLinkSlice() []LazyLink {
	orig := make([]LazyLink, 5)
	orig[1] = *GenTestLazyLink()
	orig[3] = *GenTestLazyLink()
	return orig
}
