// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package internal

import (
	"sync"

	"go.opentelemetry.io/collector/pdata"
	"go.opentelemetry.io/collector/pdata/internal/json"
)

// SpanLink is a pointer from the current span to another span in the same trace or in a
// different trace.
// See Link definition in OTLP: https://github.com/open-telemetry/opentelemetry-proto/blob/main/opentelemetry/proto/trace/v1/trace.proto
type LazySpanLink struct {
	bytes []byte
	SpanLink
}

var (
	protoPoolLazySpanLink = sync.Pool{
		New: func() any {
			return &LazySpanLink{}
		},
	}
)

func NewLazySpanLink() *LazySpanLink {
	if !UseProtoPooling.IsEnabled() {
		return &LazySpanLink{}
	}
	return protoPoolLazySpanLink.Get().(*LazySpanLink)
}

func DeleteLazySpanLink(orig *LazySpanLink, nullable bool) {
	if orig == nil {
		return
	}

	if !UseProtoPooling.IsEnabled() {
		orig.Reset()
		return
	}

	DeleteSpanLink(&orig.SpanLink, false)

	orig.Reset()
	if nullable {
		protoPoolLazySpanLink.Put(orig)
	}
}

func CopyLazySpanLink(dest, src *LazySpanLink) *LazySpanLink {
	// If copying to same object, just return.
	if src == dest {
		return dest
	}

	if src == nil {
		return nil
	}

	if dest == nil {
		dest = NewLazySpanLink()
	}

	CopySpanLink(&dest.SpanLink, &src.SpanLink)
	dest.bytes = src.bytes

	return dest
}

func CopyLazySpanLinkSlice(dest, src []LazySpanLink) []LazySpanLink {
	var newDest []LazySpanLink
	if cap(dest) < len(src) {
		newDest = make([]LazySpanLink, len(src))
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazySpanLink(&dest[i], false)
		}
	}
	for i := range src {
		CopyLazySpanLink(&newDest[i], &src[i])
	}
	return newDest
}

func CopyLazySpanLinkPtrSlice(dest, src []*LazySpanLink) []*LazySpanLink {
	var newDest []*LazySpanLink
	if cap(dest) < len(src) {
		newDest = make([]*LazySpanLink, len(src))
		// Copy old pointers to re-use.
		copy(newDest, dest)
		// Add new pointers for missing elements from len(dest) to len(srt).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazySpanLink()
		}
	} else {
		newDest = dest[:len(src)]
		// Cleanup the rest of the elements so GC can free the memory.
		// This can happen when len(src) < len(dest) < cap(dest).
		for i := len(src); i < len(dest); i++ {
			DeleteLazySpanLink(dest[i], true)
			dest[i] = nil
		}
		// Add new pointers for missing elements.
		// This can happen when len(dest) < len(src) < cap(dest).
		for i := len(dest); i < len(src); i++ {
			newDest[i] = NewLazySpanLink()
		}
	}
	for i := range src {
		CopyLazySpanLink(newDest[i], src[i])
	}
	return newDest
}

func (orig *LazySpanLink) Reset() {
	*orig = LazySpanLink{}
}

// MarshalJSON marshals all properties from the current struct to the destination stream.
func (orig *LazySpanLink) MarshalJSON(dest *json.Stream) {
	orig.SpanLink.MarshalJSON(dest)
}

// UnmarshalJSON unmarshals all properties from the current struct from the source iterator.
func (orig *LazySpanLink) UnmarshalJSON(iter *json.Iterator) {
	orig.SpanLink.UnmarshalJSON(iter)
}

func (orig *LazySpanLink) SizeProto() int {
	return orig.SpanLink.SizeProto()
}

func (orig *LazySpanLink) MarshalProto(buf []byte) int {
	return orig.SpanLink.MarshalProto(buf)
}

func (orig *LazySpanLink) UnmarshalProto(buf []byte) error {
	return orig.UnmarshalProtoOpts(buf, &pdata.DefaultUnmarshalOptions)
}

func (orig *LazySpanLink) UnmarshalProtoOpts(buf []byte, opts *pdata.UnmarshalOptions) error {
	if !opts.LazyDecoding {
		return orig.SpanLink.UnmarshalProtoOpts(buf, opts)
	}
	if err := SkipSpanLinkProto(buf); err != nil {
		return err
	}
	orig.bytes = buf
	orig.SpanLink.Reset()
	return nil
}

func SkipLazySpanLinkProto(buf []byte) error {
	return SkipSpanLinkProto(buf)
}

func (orig *LazySpanLink) FinishUnmarshal(buf *SpanLink) (*SpanLink, error) {
	if len(orig.bytes) == 0 {
		return &orig.SpanLink, nil
	}
	err := buf.UnmarshalProto(orig.bytes)
	return buf, err
}

func GenTestLazySpanLink() *LazySpanLink {
	orig := NewLazySpanLink()
	orig.SpanLink = *GenTestSpanLink()
	return orig
}

func GenTestLazySpanLinkPtrSlice() []*LazySpanLink {
	orig := make([]*LazySpanLink, 5)
	orig[0] = NewLazySpanLink()
	orig[1] = GenTestLazySpanLink()
	orig[2] = NewLazySpanLink()
	orig[3] = GenTestLazySpanLink()
	orig[4] = NewLazySpanLink()
	return orig
}

func GenTestLazySpanLinkSlice() []LazySpanLink {
	orig := make([]LazySpanLink, 5)
	orig[1] = *GenTestLazySpanLink()
	orig[3] = *GenTestLazySpanLink()
	return orig
}
