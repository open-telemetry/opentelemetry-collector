// Copyright The OpenTelemetry Authors
// SPDX-License-Identifier: Apache-2.0

// Code generated by "pdata/internal/cmd/pdatagen/main.go". DO NOT EDIT.
// To regenerate this file run "make genpdata".

package pcommon

import (
	"testing"
	"unsafe"

	"github.com/stretchr/testify/assert"

	"go.opentelemetry.io/collector/pdata/internal"
	otlpresource "go.opentelemetry.io/collector/pdata/internal/data/protogen/resource/v1"
)

func TestResourceEntityRefSlice(t *testing.T) {
	es := NewResourceEntityRefSlice()
	assert.Equal(t, 0, es.Len())
	state := internal.StateMutable
	es = newResourceEntityRefSlice(&[]*otlpresource.ResourceEntityRef{}, &state)
	assert.Equal(t, 0, es.Len())

	emptyVal := NewResourceEntityRef()
	testVal := ResourceEntityRef(internal.GenerateTestResourceEntityRef())
	for i := 0; i < 7; i++ {
		el := es.AppendEmpty()
		assert.Equal(t, emptyVal, es.At(i))
		internal.FillTestResourceEntityRef(internal.ResourceEntityRef(el))
		assert.Equal(t, testVal, es.At(i))
	}
	assert.Equal(t, 7, es.Len())
}

func TestResourceEntityRefSliceReadOnly(t *testing.T) {
	sharedState := internal.StateReadOnly
	es := newResourceEntityRefSlice(&[]*otlpresource.ResourceEntityRef{}, &sharedState)
	assert.Equal(t, 0, es.Len())
	assert.Panics(t, func() { es.AppendEmpty() })
	assert.Panics(t, func() { es.EnsureCapacity(2) })
	es2 := NewResourceEntityRefSlice()
	es.CopyTo(es2)
	assert.Panics(t, func() { es2.CopyTo(es) })
	assert.Panics(t, func() { es.MoveAndAppendTo(es2) })
	assert.Panics(t, func() { es2.MoveAndAppendTo(es) })
}

func TestResourceEntityRefSlice_CopyTo(t *testing.T) {
	dest := NewResourceEntityRefSlice()
	// Test CopyTo to empty
	NewResourceEntityRefSlice().CopyTo(dest)
	assert.Equal(t, NewResourceEntityRefSlice(), dest)

	// Test CopyTo larger slice
	generateTestResourceEntityRefSlice().CopyTo(dest)
	assert.Equal(t, generateTestResourceEntityRefSlice(), dest)

	// Test CopyTo same size slice
	generateTestResourceEntityRefSlice().CopyTo(dest)
	assert.Equal(t, generateTestResourceEntityRefSlice(), dest)
}

func TestResourceEntityRefSlice_EnsureCapacity(t *testing.T) {
	es := generateTestResourceEntityRefSlice()

	// Test ensure smaller capacity.
	const ensureSmallLen = 4
	es.EnsureCapacity(ensureSmallLen)
	assert.Less(t, ensureSmallLen, es.Len())
	assert.Equal(t, es.Len(), cap(*es.getOrig()))
	assert.Equal(t, generateTestResourceEntityRefSlice(), es)

	// Test ensure larger capacity
	const ensureLargeLen = 9
	es.EnsureCapacity(ensureLargeLen)
	assert.Less(t, generateTestResourceEntityRefSlice().Len(), ensureLargeLen)
	assert.Equal(t, ensureLargeLen, cap(*es.getOrig()))
	assert.Equal(t, generateTestResourceEntityRefSlice(), es)
}

func TestResourceEntityRefSlice_MoveAndAppendTo(t *testing.T) {
	// Test MoveAndAppendTo to empty
	expectedSlice := generateTestResourceEntityRefSlice()
	dest := NewResourceEntityRefSlice()
	src := generateTestResourceEntityRefSlice()
	src.MoveAndAppendTo(dest)
	assert.Equal(t, generateTestResourceEntityRefSlice(), dest)
	assert.Equal(t, 0, src.Len())
	assert.Equal(t, expectedSlice.Len(), dest.Len())

	// Test MoveAndAppendTo empty slice
	src.MoveAndAppendTo(dest)
	assert.Equal(t, generateTestResourceEntityRefSlice(), dest)
	assert.Equal(t, 0, src.Len())
	assert.Equal(t, expectedSlice.Len(), dest.Len())

	// Test MoveAndAppendTo not empty slice
	generateTestResourceEntityRefSlice().MoveAndAppendTo(dest)
	assert.Equal(t, 2*expectedSlice.Len(), dest.Len())
	for i := 0; i < expectedSlice.Len(); i++ {
		assert.Equal(t, expectedSlice.At(i), dest.At(i))
		assert.Equal(t, expectedSlice.At(i), dest.At(i+expectedSlice.Len()))
	}
}

func TestResourceEntityRefSlice_RemoveIf(t *testing.T) {
	// Test RemoveIf on empty slice
	emptySlice := NewResourceEntityRefSlice()
	emptySlice.RemoveIf(func(el ResourceEntityRef) bool {
		t.Fail()
		return false
	})

	// Test RemoveIf
	filtered := generateTestResourceEntityRefSlice()
	pos := 0
	filtered.RemoveIf(func(el ResourceEntityRef) bool {
		pos++
		return pos%3 == 0
	})
	assert.Equal(t, 5, filtered.Len())
}

func TestResourceEntityRefSlice_Sort(t *testing.T) {
	es := generateTestResourceEntityRefSlice()
	es.Sort(func(a, b ResourceEntityRef) bool {
		return uintptr(unsafe.Pointer(a.getOrig())) < uintptr(unsafe.Pointer(b.getOrig()))
	})
	for i := 1; i < es.Len(); i++ {
		assert.Less(t, uintptr(unsafe.Pointer(es.At(i-1).getOrig())), uintptr(unsafe.Pointer(es.At(i).getOrig())))
	}
	es.Sort(func(a, b ResourceEntityRef) bool {
		return uintptr(unsafe.Pointer(a.getOrig())) > uintptr(unsafe.Pointer(b.getOrig()))
	})
	for i := 1; i < es.Len(); i++ {
		assert.Greater(t, uintptr(unsafe.Pointer(es.At(i-1).getOrig())), uintptr(unsafe.Pointer(es.At(i).getOrig())))
	}
}

func generateTestResourceEntityRefSlice() ResourceEntityRefSlice {
	return ResourceEntityRefSlice(internal.GenerateTestResourceEntityRefSlice())
}
