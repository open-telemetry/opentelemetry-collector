receivers:
  template/my_filelog_template:
    my_file: foo.log
exporters:
  otlp:
    endpoint: localhost:4317

service:
  pipelines:
    logs:
      receivers: [template/my_filelog_template]
      exporters: [otlp]
