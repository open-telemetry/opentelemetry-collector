receivers:
  filelog/my_filelog_template:
    include: foo.log
processors:
  batch:
exporters:
  otlp:
    endpoint: localhost:4317

service:
  pipelines:
    logs:
      receivers: [filelog/my_filelog_template]
      processors: [batch]
      exporters: [otlp]
